# HISTORY — benchmar_assign_taxo

**This file is the archive, not the todo.** It is the ROADMAP as it stood on
2026-09-22, kept verbatim: every ticked item with its dated narrative and its
measurements, every past decision (sections 3, 3b, 3c), and the pre-2026-09-17
reference material (section 4). It was split out of `ROADMAP.md` on 2026-09-22
(item C22-P19 of [`critique_2026-09-22.md`](critique_2026-09-22.md) §2.3: the
ROADMAP had become "a changelog wearing a todo's clothes", 24 lines above
1 200 characters).

**The running todo is [`ROADMAP.md`](ROADMAP.md)**, one line per open item. Every
item there points back to the section of this file that carries its history, so
nothing was deleted in the split — only the open items were lifted out and
shortened. When an item of `ROADMAP.md` is finished, move its narrative here.

Other entry points: [`AGENTS.md`](AGENTS.md) (how to work in this project),
[`CONTEXT.md`](CONTEXT.md) (glossary), `docs/objectives_design.md` (the design,
source of truth).

---

Working plan derived from `Coffre_principal/PROJETS/Projets IdEst/Dev packages R/MiscMetabar/autour_de_MiscMetabar/benchmark_taxo_assign/` (`Taxonomic assignation manuscript.md` + `Taxonomic assignation.md`).

**How this file was organised as a ROADMAP** (restructured 2026-09-10, section 0 added 2026-09-17):

0. [Implementation plan of the 2026-09-17 design](#0-implementation-plan-of-the-2026-09-17-design) — what the code must change to match `docs/objectives_design.md` and `docs/hleap_2021_metrics.md`; start here.
1. [Structure & maintenance](#1-structure--maintenance-critique-fable-2026-09-10) — engineering debt found by the structural review in [`critique_fable/`](critique_fable/README.md). Ordered in execution phases; each item links to the evidence.
2. [Open scientific work](#2-open-scientific-work) — every unticked manuscript item (D1a figures, D1b, D1c, M1.2, C1, C4, side questions).
3. [Decisions](#3-decisions-taken-2026-05-19) — the seven scope decisions of 2026-05-19 and the six structural decisions of 2026-09-10 and the four database decisions of 2026-09-11 (section 3b), the design decisions of 2026-09-17 that supersede some of them (section 3c), and the grid and framing decisions of 2026-09-22 (section 3d); do not reopen without a new dated entry.
4. [Reference](#4-reference) — research questions, dataset matrix, suggested execution order.
5. [Done log](#5-done-log) — every ticked item, kept verbatim for traceability.

Tags follow the workspace convention: `[Priority / facility]`, Priority = Critical → High → Medium → Low, facility = easy (< 1 day) → moderate (a few days) → hard.

---

## 0. Implementation plan of the 2026-09-17 design

Source of truth: [`docs/objectives_design.md`](docs/objectives_design.md) (decisions 1–27, written "OD N" below, **not** the ROADMAP decisions of section 3) and [`docs/hleap_2021_metrics.md`](docs/hleap_2021_metrics.md) §5–6. `docs/experimental_design.md` keeps the measured costs and the ITS-region findings, but its counts predate this design (item 0.10).

**Scope (OD 12):** taxonomic assignment of Fungi, nothing else. The design in one table:

| Axis | Decided | Today in the code |
|---|---|---|
| Databases | `[EUK_ITS_v2.1 \| Unite_all_20250219 \| Unite_s_all_20250219] × [all \| fungi \| fungi + rep]` (OD 1, 3, 13, 14, 21) | 7 databases, including `EUK_ITS_v2.1_Fungi_cut` |
| Trimmed databases | none in the grid; `fungi + rep cut` as a final zoom (OD 16, 24, 25) | `_Fungi_cut` in the grid |
| Inputs | ASV; OTU `d_vs` assigned directly, no MUMU, no ITSx (OD 4, 19); ITSx ASV on fungi and fungi + rep | ASV and ITSx; OTU taxonomy derived from member ASVs |
| Computations per dataset | 96 = ASV 36 + OTU 36 + ITSx 24 | 44 |
| Parameter rows per computation | 21: dada2 3, sintax 3, lca 3 (`lca_cutoff` ∈ {0.8, 0.9, 1}, OD 9, 18, 23), blastn 3 votes × `min_id` ∈ {90, 92, 95, 97} (OD 10, 17) | 12 (lca rows are copies, blastn `min_id` fixed) |
| Controls | `fake_` (`prop_fake = 0.5`) and 100 `external_` on every input; `external_` enter TN on fungi databases only (OD 6, 22) | `fake_` and `external_` enter TN everywhere |
| Truth of the mocks | one lineage per unit, from the Sanger sequence of each strain (hleap §6) | a set of values per rank |
| Metrics | Hleap cells per unit and rank; PPV, TPR, F1, MCC, TNR = TN / (TN + FP); `NA_real`, `NA_fake`, `ext_fungi`, `ext_correct`, `ctrl_assigned`, `misassign_seq`; abundance-filter sweep (OD 7, 11; hleap §5) | `tc_metrics_mock_vec()`: set membership, TNR = TN / (TN + FN) |
| Cross-validation | database subset decided after the mock results (OD 27); fungal records queried only (OD 20) | 7 databases × 4 methods × 2 variants |

**Order.** 0.1 before anything else: `config.R::benchmark_dbs` feeds `db_list`, `db_meta`, `itsx_db_list`, `values_map`, both pipelines, the chapters and the tests. Then 0.2 (databases on disk) and 0.3 (MiscMetabar) independently, then 0.4–0.5 (targets), 0.6–0.7 (truth and scoring, independent of the runs), 0.8 (chapters). **No production run before 0.1–0.5 are done**: a run on the current grid would be thrown away. Warn the developer before any run (96 computations per dataset, measured cost of the 44 current ones: 2.15 h of target time, dada2 53 %, 53.1 GB peak).

### 0.1 Grid in `config.R`

- [x] **0.1a** `[Critical / easy]` `benchmark_dbs`: drop `EUK_ITS_v2.1_Fungi_cut`, add the three `fungi + rep` databases; `itsx_db_list` = fungi and fungi + rep (6 databases). `EUK_ITS_v2.1_Fungi_cut` stays on disk for the zoom (0.11), and `pipelines/cross_val.R` keeps its `_cut` query rule. **Done 2026-09-17**: `benchmark_dbs` = 3 sources × {full, Fungi, Fungi+rep} (`_Fungi_rep`); `config.R::zoom_dbs` holds `EUK_ITS_v2.1_Fungi_cut`, still built by `derive_all_variants()`; `config.R::cv_db_list` keeps the 7 former databases for the CV, whose 56 targets are unchanged (OD 27); `rep_share = 0.05`.
- [x] **0.1b** `[Critical / easy]` `bio_datasets` still maps `tedersoo_illumina` to a `_Fungi_cut_full_ITS` database, substituted into `db_list`, `itsx_db_list` and `db_meta$db` when `assign_taxo_tedersoo_illumina` runs (`docs/experimental_design.md` §5.3). Remove the substitution: Tedersoo uses the same nine databases. Do not run `assign_taxo_tedersoo_illumina` before this. **Done 2026-09-17**: the swap block is removed and `bio_datasets$cut_suffix` is `NA` (the column stays, `derive_all_variants()` reads it); `tar_manifest()` of `assign_taxo_tedersoo_illumina` gives the same 96 computations and 504 rows as `assign_taxo`.
- 0.1a and 0.1b change `db_list`, hence every target name holding a database: update `tests/test_values_map.R` in the same change (it requires `setequal(db_meta$db, db_list)`), and expect `tar_prune()` to drop the `EUK_ITS_v2.1_Fungi_cut` objects of `store_assign_taxo` on the next run.

### 0.2 `fungi + rep` databases

- [x] **0.2** `[Critical / moderate]` Derivation in `make_databases.R`, idempotent, with tests (OD 3, 13, 14, 21; allocations in objectives_design §1.3):
  - reps = 5 % of the fungal records of the database: 53 844 for EUKARYOME, 5 107 for UNITE, 8 402 for UNITE_sh;
  - drawn from the "all" release of the same source;
  - kingdoms with ≥ 10 records (UNITE, UNITE_sh) or ≥ 100 (EUKARYOME), after `config.R::rep_kingdom_exclude` (already in `config.R`, with `rep_kingdom_min_records` and `kingdom_aliases`);
  - equal share per kingdom by water-filling (a kingdom smaller than the cap gives all its records);
  - the 100 `external_` records are never drawn: by accession for UNITE, **by sequence for EUKARYOME** (other identifiers);
  - seed `targets_seed`; dada2 and sintax formats; `mini_*` subsets.
  The `[Critical / easy]` "adding only some sequences to a FUNGI filtered database" item of section 2 is this item.
  **Done 2026-09-17**: `make_databases.R::water_fill()` and `derive_fungi_rep()` (tests on tiny fastas), called by `derive_all_variants()` after the fake reference. The exclusion of the `external_` controls is by identical sequence for every release. The three databases are built (23 s): 107 244, 176 432 and 1 130 714 records in both formats (5 107, 8 402 and 53 844 representatives), `mini_*` = 10 000 Fungi + 500 representatives, drawn records in `sources/<db>_reps.csv`. A few allocations differ from objectives_design §1.3 because the excluded controls leave the candidate pool first (recorded there).
  - [x] **0.2b** `[Medium / easy]` **Decided 2026-09-22 (developer): a representative must be below 97 % identity with every `external_` control**, and the rule applies to the representatives only — the whole releases keep the controls (they are records of `fake_ref_source`, which is what makes `ext_correct` computable there) and the `_Fungi` databases hold none. Measured before the fix (vsearch, the 100 controls against each database): whole releases 100 / 58 / 78 controls with a hit ≥ 97 % (UNITE_sh / UNITE / EUKARYOME), `_Fungi` and `_Fungi_cut` 0, `_Fungi_rep` 6 / 7 / 10. Implemented in `derive_fungi_rep()` (`config.R::rep_max_identity_to_external`, `reps_near_external()`): the drawn records are searched against the controls with vsearch, those too close are dropped from the candidates and redrawn until none is left; test in `tests/test_make_databases_helpers.R` (a control and a one-base variant of it are never drawn). The exclusion is one vsearch pass per database, with the candidate records as queries and the 100 controls as database (the other direction takes tens of minutes on a whole release): 47, 47 and 403 candidates excluded, 35 min for the three databases, 31 of them for EUKARYOME's 731 034 candidates. Rebuilt and checked 2026-09-22: 0 control with a hit at >= 97 % in the three `_Fungi_rep` databases, unchanged elsewhere. A first implementation redrew the representatives until none was too close, which only saw the drawn records (18 and 16 dropped on the UNITE databases) and took more than 20 min on EUKARYOME; it was replaced by the single pass above, which sees every candidate. **The `_Fungi_rep` assignments of `store_assign_taxo_mini` predate the rebuild** (235 outdated targets, 231 of them `_Fungi_rep`): rerun `assign_taxo_mini` before quoting any number from them. Production rebuilds everything anyway.

### 0.3 lca cutoff sweep (MiscMetabar)

- [x] **0.3** `[High / moderate]` `MiscMetabar::assign_vsearch_lca()` returns or saves the `--userout` hit table (`query+id+target`) and accepts it back through `hits_table =` to apply any `lca_cutoff` without running vsearch, mirroring `blast_pq()` / `assign_blastn(blast_table = )` (OD 18, 23). The derivation needs its own **fraction** rule (`resolve_vector_ranks(nb_agree_threshold = )` takes a count). Equivalence test: cutoff 1 in R reproduces the `--lcaout` columns; also test ties between several top hits (not exercised by the 2026-09-17 check). Tests, NEWS, man in the MiscMetabar checkout. Costs one lca rerun per database × input, absorbed by the new grid. **Done 2026-09-17** in MiscMetabar (`R/vsearch.R`, uncommitted): `behavior = "return_hits"` returns `query`, `id`, `target` (plus one NA row per query without hit), `hits_table =` computes the taxonomy without vsearch. The LCA follows `results_show_lcaout()` of vsearch 2.31.0 (`src/results.cc`): per level d…t, most frequent lineage prefix, stop at the first level below the cutoff, skip empty names; vsearch's Boyer-Moore candidate equals the most frequent prefix since `lca_cutoff` > 0.5, so the result does not depend on hit order. Identical to vsearch `--lcaout` on `data_fungi_mini` for cutoffs 0.51, 0.6, 0.8, 0.9, 1, with and without `top_hits_only`, with 1 and 2 threads; package test on a synthetic reference with heterogeneous identical top hits (genus kept at 0.6, not at 0.9) and an empty rank. NEWS, man. **Caveat for the sweep:** with `top_hits_only = TRUE`, 0.8 and 0.9 only differ from 1 when at least 5 (0.8) or 10 (0.9) top hits tie. On the mini store (10 000-record references), 3–8 of 392 queries have several top hits and every lca column is identical at 0.8 and 1. On the full databases it is not inert (the 196 Pauvert ASVs, `return_hits` + `.lca_from_hits()`, 2026-09-17): `EUK_ITS_v2.1_Fungi` median 4 top hits, 94 ASVs with ≥ 5, 48 lineages differ between 0.8 and 1, genus assigned to 148 / 140 / 125 ASVs at 0.8 / 0.9 / 1; `Unite_s_all_20250219_Fungi` median 1 top hit, 4 lineages differ, genus 179 / 178 / 178.

### 0.4 `values_map` and targets

- [x] **0.4a** `[Critical / moderate]` `R/values_map.R`: OTU input (`preprocess` value for `d_vs`), blastn rows × `min_id`, three lca rows reading the stored hit table; 21 rows per computation. Update `tests/test_values_map.R`, which pins the target names (new names are expected; existing ones must not change unless the grid drops them). **Done 2026-09-17**: names decided by the developer: `lca__DB___0.8` (cutoff in the parameter slot), `blastn__DB___0.5...rel_majority...100...95` (`min_id` appended), `otu_<method>__…`; 21 rows per database × input (not per computation), 96 computations, 504 rows; dada2 / sintax names unchanged.
- [x] **0.4b** `[High / moderate]` `R/assign_compute.R`: lca computation keeps the hit table; `derive_assignment()` applies `lca_cutoff` and blastn `min_id`. **Done 2026-09-17**, with `tests/test_assign_compute.R` (derived lca rows at 0.8 and 1 equal `add_new_taxonomy_pq(method = "lca")`, derived blastn rows equal `assign_blastn(min_id = )`).
- [x] **0.4c** `[High / moderate]` `pipelines/assign_taxo.R`: OTU branch `d_vs` → shuffled → `external_` controls under `withr::with_seed(targets_seed, …)` (OD 4; `add_shuffle_seq_pq()` needs a `sample_data` with ≥ 2 variables); `d_vs` comes from the dataset's DADA2 store (`store_dada2`, `store_dada2_<dataset>`), not `d_vs_mumu`. `combine_taxo_assignments()` needs the same taxa in every input it combines: one combined object per input. **Done 2026-09-17**: `d_vs_file` → `d_vs` → `d_vs_shuffled` → `d_vs_for_assignation`; `d_all_taxo` (ASV + ITSx rows) and `d_all_taxo_otu` (OTU rows). Also in `analysis/`: `db_shapes` has 9 shapes; chapter 01 parses `lca_cutoff` and `min_id` into their own columns; `filter_default_settings()` keeps lca at `lca_cutoff = 1` and blastn at `min_id = 95`; the Q1.4 vote figure is drawn at `min_id = 95`. `assign_taxo_mini` run 2026-09-17: 640 targets, 8 min 39, 0 error, 0 warning; `d_all_taxo` 392 taxa, `d_all_taxo_otu` 314 taxa (143 OTUs, 71 `fake_`, 100 `external_`); blastn genus assignments fall with `min_id` (e.g. 53 / 52 / 41 / 32 ASVs at 90 / 92 / 95 / 97 on `mini_Unite_s_all_20250219`). Chapters not re-rendered.
- [x] **0.4d** `[Medium / easy]` **Decided 2026-09-22 (developer): the Q3 consensus votes over the default settings only**, one column per method and database (4 x 9 = 36, equal weight), instead of the 189 columns of the full sweep where blastn alone would weigh 57 %. Implemented in `analysis/01_load_and_clean.qmd`: the other columns (other parameter values, ITSx rows) are set aside before `resolve_taxo_conflict()` and added back after. The four default rules are now written twice, there and in `filter_default_settings()` (`analysis/00_setup.R`): merge them into one definition when 0.8a rewrites chapter 01. Former wording: Q3 consensus: `resolve_taxo_conflict()` in chapter 01 now votes over 9 databases, 3 lca and 12 blastn columns each (blastn weighs 4× more than before). Decide which columns enter each consensus before rendering Q3.
### 0.5 ITSx region per dataset

- [x] **0.5** `[High / moderate]` Per-dataset `itsx_region` (ITS1 for Pauvert, full ITS for Tedersoo; `docs/experimental_design.md` §5.1): today a global constant read once by `pipelines/assign_taxo.R`. **Done 2026-09-17**: `bio_datasets$itsx_region` (`full` for Tedersoo), `config.R::itsx_region` reads it; `run_itsx(region = "full")` passes `--save_regions none` and reads ITSx's `<prefix>.full.fasta` (ITS1 + 5.8S + ITS2; on the tested Tedersoo ASV it spans positions 155–657, one base past the ITS2 end of the positions table); test in `tests/test_itsx.R`. Not run on the Tedersoo store yet.

### 0.6 Per-unit truth of the mocks (M1.4)

- [x] **0.6** `[Critical / moderate]` A function (with tests) that builds one truth lineage per unit of a mock, from its Sanger table (`data/data_raw/metadata/pauvert2019_table_s1_mock_sanger.csv`, `tedersoo_mock_table_s1_sanger.csv`):
  - match: vsearch identity ≥ 99.5 % **and** the shorter of the two sequences covered at ≥ 99.5 % (`max(qcov, tcov)`, confirmed Q2a); expected 149 / 196 Pauvert ASVs (95.6 % of reads) and 99 / 238 Tedersoo ASVs (87.6 %);
  - ties between strains: lowest common rank, unit left out below it (Q2c);
  - units without truth: kept, FN if NA, FP otherwise, and counted in `NA_real` (Q2b);
  - names: submitted name is the truth; the submitted **or** the GBIF accepted name counts as correct at Species and Genus; ranks above Genus from the GBIF lineage, `taxinfo::gna_verifier_pq(data_sources = 11, classification_col = TRUE)` (option added 2026-09-17, uncommitted in taxinfo); Table S1 disagreements of Pauvert checked by hand (hleap §6);
  - EUKARYOME stores the epithet only at Species: compare genus + epithet;
  - OTU input: the truth of the OTU's representative sequence, matched the same way.
  Details and measurements: M1.4 in section 2.
  **Done 2026-09-22**: `R/mock_truth.R` (`read_mock_sanger()`, `refresh_gna_lineage_cache()`, `mock_lineage()`, `sanger_matches()`, `mock_truth_table()`, `build_mock_truth()`, `lineage_disagreements()`), `config.R::mock_sanger_tables` (one row per mock: table, separator, columns, lineage cache), `tests/test_mock_truth.R`. The table has one row per unit: the rank columns, `truth_depth` (deepest rank with a truth, NA when nothing matched — a unit is scored down to it and leaves the matrix below), `Genus_accepted` / `Species_accepted` (the GBIF accepted name, which counts as correct too), `n_strains` and `strains`. Reproduces the confirmed counts: 149 / 196 Pauvert ASVs with a truth (12 ties), 99 / 238 Tedersoo (3 ties), 126 / 143 and 74 / 180 on the OTUs. The lineages come from two caches written once from the GNA verifier (`data/data_raw/metadata/*_gna_lineage.csv`, 181 and 103 names), so the builder needs no network; a name missing from a cache is an error, never a silent NA. Corrected 2026-09-22 while building 0.8a: `species_name()` replaces `binomial_name()` for the Species columns, so a strain named "Genus sp" carries no species truth and stops at Genus (10 Pauvert units; see 0.8a). Open for the developer: the 61 disagreements between the Pauvert Table S1 lineage and GBIF (`data/data_final/mock_truth/pauvert_lineage_disagreements.csv`), and the two Pauvert units whose truth stops above Genus (a strain named to class only, and a `Fusarium sp` tie that GBIF misplaces).

### 0.7 Scoring (comparpq)

- [x] **0.7** `[Critical / moderate]` Scoring as `TA_pipes::optimize_n_score.py::score()` (hleap §6), in comparpq: one cell per unit and rank (real = own truth → TP; other name → FP; NA → FN; `fake_` named → FP, NA → TN); `external_` in the matrix on fungi databases only, otherwise `ext_fungi` / `ext_correct` (`ext_correct` at Kingdom only on fungi + rep, OD 15); TN per rank; TNR = TN / (TN + FP) with every FP (Q2d); the metrics of OD 7. Takes a truth **per taxon**, not a set per rank. Tests, NEWS. Q2 comparisons across simplification levels rest on PPV, TPR, F1, `NA_real` and control rates, not ACC / MCC (their margins change with the database, objectives_design §1.2).
  **Done 2026-09-22** in comparpq (`R/compare_taxo_unit.R`, uncommitted): `tc_metrics_unit_vec()` scores one column at one rank and `tc_metrics_unit()` every column at every rank, returning the same long data.frame as `tc_metrics_mock()`. `tc_metrics_mock()` and `tc_metrics_mock_vec()` are left untouched, with their set-membership scoring, for a mock that only comes with a list of expected taxa; nothing had to change in their test. The new functions take the truth table of 0.6 (`truth_depth` decides where a unit leaves the matrix), accept the `<rank>_accepted` name as correct too, and place the external controls in the matrix or aside through `external_scoring` (decisions 6, 15, 22). Metrics: the four cells, FDR, PPV, TPR, **TNR = TN / (TN + FP) with every FP**, F1, ACC, MCC (0 on a zero margin), plus `NA_real`, `NA_fake`, `ctrl_assigned_fake`, `ctrl_assigned_ext`, `misassign_seq`, `n_real`, `n_left_out`, and `ext_fungi` / `ext_correct` from the wrapper (`external_correct_ranks` restricts the latter to the kingdom on a `fungi + rep` database). The project reads the controls' own lineage from their names with `external_control_truth()` (`R/mock_truth.R`). Checked on the mini store: every scored unit falls in exactly one cell, and on `mini_Unite_s_all_20250219_Fungi` every external control is called Fungi (`ext_fungi` = 1, `ext_correct` = 0) while lca on the whole release calls 20 % of them Fungi and names 69 % correctly. Tests: `comparpq/tests/testthat/test-compare_taxo_unit.R`, NEWS, `_pkgdown.yml`, man; DESCRIPTION and NAMESPACE kept as they were (roxygen 8.1.0 reformats them), with the two export lines added by hand.

### 0.8 Analysis

- [x] **0.8a** `[High / moderate]` `analysis/01_load_and_clean.qmd`: read the OTU assignments instead of `resolve_otu_taxonomy()`; call the new scoring on the per-unit truths; one dataset column (Pauvert, Tedersoo). Three things settled while building 0.6 and 0.7, to apply here:
  - the truth comes from `build_mock_truth()` on the DADA2 store of the dataset — `d_asv` for the ASV and ITSx inputs (the ITSx input carries the same taxa, so it must not be re-matched on its trimmed sequences) and `d_vs` for the OTU input (126 / 143 Pauvert OTUs and 74 / 180 Tedersoo OTUs get a truth). A `mock_truth` target reading `config.R::mock_sanger_tables` is the natural place;
  - the Species row of `ranks_df` is the `Gen_sp_<full_name>` column, not a second Genus column: `truth$Species` is the underscored binomial (EUKARYOME stores the epithet alone, which is why chapter 01 builds `Gen_sp_*`);
  - `external_scoring` is per database, so the columns must be scored in three groups (`db_meta$simplification`): `"matrix"` on the three `_Fungi`, `"aside"` on the three whole releases and on the three `_Fungi_rep`, the latter with `external_correct_ranks = "Kingdom"` (objectives_design decisions 6, 15, 22). One call per group.
  **Done 2026-09-22.** `analysis/01_load_and_clean.qmd` rewritten around three inputs told apart by a `unit` column, and `R/score_assignments.R` (with `tests/test_score_assignments.R`) added: `assignment_db()` reads the database out of a target name, `scoring_groups()` gives each column the `external_scoring` its simplification imposes, and `score_assignments()` calls `comparpq::tc_metrics_unit()` once per group. The truth is built in the chapter, not as a target: nothing outside chapter 01 reads it, and the target only starts paying once a production store exists. New in `00_setup.R`: `analysis_datasets` (one row per dataset whose store exists, so the chapter renders before the Tedersoo run), `enrich_metrics()` and `control_pattern` moved out of the chapter, and `default_view()`.
  Two decisions of the developer (2026-09-22) shaped it: the **consensus columns are scored twice**, `"matrix"` and `"aside"` (they have no database of their own, and both readings are defensible), and the **OTU taxonomy derived from the member ASVs is kept** beside the directly assigned OTUs, so Q5 now compares "assign then cluster" with "cluster then assign" (`unit` = `OTU_derived` against `OTU`).
  **Chapters 02 to 05 read `cache_read("res_comp_tax") |> default_view()`**: without it they would average the three inputs and count every consensus column twice, silently. Q4, Q5 and the dataset comparison read the cache directly.
  **A bug of 0.6 surfaced here and is fixed**: 10 Pauvert strains are named "Genus sp", and `binomial_name()` turned that into a species truth (`Phoma_sp`) that no assignment can ever match, since the chapter's own cleaning turns every `<Genus>_sp` assignment into NA. `R/mock_truth.R::species_name()` now returns NA for a placeholder epithet (`sp`, `spp`, `cf`, `aff`, `indet`, `incertae`) or a name with no epithet, so those units stop at Genus and leave the matrix at Gen_sp, exactly as a tie does. Pauvert `truth_depth` goes from 137 / 10 / 1 / 1 / 47 (Species / Genus / Phylum / Class / none) to 127 / 20 / 1 / 1 / 47; `external_control_truth()` uses the same rule, which changes no `ext_correct` (the controls' own `s__X_sp` names were unmatchable too).
  Checked on `store_assign_taxo_mini` (2 min 30, every chapter 01 to 05 renders): 106 572 metric rows, 149 / 196 ASVs and 126 / 143 OTUs with a truth as 0.6 measured, the four cells summing to the scored units in every group (ASV 390 = 193 real + 2 left out ... + 197 controls at `"matrix"`), and the `Gen_sp` spellings matching (`Fusarium_equiseti`, `Rhizoctonia_solani`), with TP > 0 on the best databases. Numbers from the mini databases are smoke-test numbers: quote nothing from them.
  Guards added while reviewing: chapters 03 and 05 stop when no `benchmark_costs$phase` matches `res_comp_tax$method_db` (a store predating the grid would have joined to nothing, silently); `default_view()` falls back to the first dataset of the table it is given, so it does not need `analysis_datasets` to be non-empty. Checked at the Kingdom rank: the truth is `Fungi` for all 149 units that have one, FN = 0, and TN = 0 on a Fungi-only database because every query gets a kingdom there — which is what `ext_fungi` measures, not a scoring failure.
- [x] **0.8b** `[Medium / easy]` Abundance-filter sweep (M1.5), mocks and biological datasets only. **Done 2026-09-22**: section "M1.5 - abundance filter" of chapter 01, `res_abundance` cached. 17 thresholds (1, then 2 to 10, 12, 15, 20, 30, 40, 50, 100), on the default settings only (4 methods x 9 databases, the columns the Q3 consensus votes over) and on the ASV and OTU inputs, which is 16 extra scoring passes; the Q1 parameter sweeps are read at one threshold.
  **The filter applies to the real units only.** The negative controls carry no reads at all (they are injected into the tax_table and the refseq with a zero count), so filtering them with the rest removed all 197 of them at the very first threshold and emptied the TN column - TNR fell to 0/0 between n = 1 and n = 2 and read as an effect of the filter. Found on the first run and fixed with `config.R`-free `control_pattern` (`00_setup.R`). On the mini store the Pauvert mock keeps 195 / 194 / 185 / 171 / 122 ASVs at n = 1 / 3 / 8 / 20 / 100 (143 / 142 / 133 / 124 / 96 OTUs), and TNR rises from 0.849 to 0.929 while F1 stays within 0.46-0.50: the filter removes FP-generating rare units, it does not change the ranking.
### 0.9 In silico without miaSim

- [x] **0.9** `[Medium / easy]` Remove the miaSim / Hubbell branch (OD 12): `analysis/in_silico_simulation.qmd` (D1b-B) and D1b-B in section 2 (CONTEXT.md updated 2026-09-17). D1b keeps InSilicoSeq, source `Unite_s_all_20250219` (OD 5). **Done 2026-09-22**: the notebook goes from 573 to 190 lines and holds the InSilicoSeq branch alone (four steps: input fasta, the Docker `iss generate` recipe, the targets projects, the truth table); the miaSim probe of the setup chunk, the Hubbell simulation, its figures and the D1b-B / mock comparison are gone. `parse_unite_taxonomy()` is kept — D1b-A's truth needs it — and is now followed by a chunk that builds `insilico_truth` from `iss_input_fasta`, which is the per-unit truth `comparpq::tc_metrics_unit()` takes, not a `tc_metrics_mock()` set. New `config.R::insilico_source`: the notebook drew its sequences from `fake_ref_source`, the release that feeds the *negative controls*, which happens to be the same file for another reason. Renders end to end (`BENCHMARK_ANALYSIS_MINI=TRUE`). D1b-B's items in section 2 are replaced by a pointer to the git history plus the one finding that still applies to D1b-A (phylum coverage of a uniform draw).

### 0.10 Documents

- [x] **0.10** `[Low / easy]` `docs/experimental_design.md`: 44 computations → 96 (§1, §2, §6), in silico D1b-B row (§4), Pauvert truth "103 taxa" → 189 strain rows / 181 species (103 is Tedersoo), `_cut` implications §5.2–5.3 now moot for the grid. `docs/objectives_design.md` §5 still lists Q-a as open although hleap §5–6 settles it, and §6 lists the files in contradiction: tick them as 0.1–0.9 land.
  **Done 2026-09-22.** `docs/experimental_design.md`: §1 and §2 now read 96 computations (36 ASV + 36 OTU + 24 ITSx) and 21 parameter rows, and the objective map gains Q5's own 36 computations — Q5 changed side with decision 4, it is no longer free; §4's dataset table drops the D1b-B row, gives Pauvert its 189 strain rows / 181 species and Tedersoo its 103 species, both per unit; §5.2 and §5.3 are marked moot and fixed respectively, keeping what each still says correctly; §6's decision table is annotated. Its header now carries one warning instead of a list: **the measured costs of §3 were read on the 44-computation grid and describe less than half the current work** — they are kept as an order of magnitude because they are the only figures anyone has. `docs/objectives_design.md`: Q-a is struck from §5 with the rule that settled it, and §6's contradictions are struck one by one, except `pipelines/cross_val.R`, which waits on decision 27.

### 0.11 Later, after the first results

- [ ] **0.11a** `[Medium / easy]` Cross-validation databases (OD 27), chosen from the mock results; the CV grid stays as it is until then.
- [ ] **0.11b** `[Medium / moderate]` Final zoom (OD 16, 24, 25): `fungi + rep cut` (`_Fungi_cut` records + untrimmed reps) on the two best databases (one UNITE, one EUKARYOME) × best methods, Pauvert only. Note: trimmed records (≈ 210 bp) and untrimmed reps (> 500 bp) differ in length; compare `ext_fungi` between `fungi + rep` and `fungi + rep cut` to see whether the k-mer methods are biased.
- [ ] **0.11c** `[Low / easy]` Discussion notes: mono-nucleotide shuffle (M1.6); set-membership bias of the former metrics made moot by the per-unit truth; parameters reported on the whole mock, not tuned on a third as in Hleap (hleap §5, point 8).

---

## 1. Structure & maintenance (critique Fable, 2026-09-10)

Evidence and proposed code for every item: [`critique_fable/`](critique_fable/README.md). Ids (S0.1 …) are referenced from those files.

### Phase 0 — Quick wins (one sitting, no rerun needed)

Applied 2026-09-10. Notes: S0.1 done except the `store_*_mini` routing, which is part of S3.1 (`_targets.yaml`); `mini_db` now defaults to FALSE, so the **next `tar_make()` on `store_assign_taxo` / `store_cross_val` is a full production rerun** (hours). S0.3 changed the `file_refseq_taxo` and `filtered` commands, so `store_dada2` will also rebuild from `filtered` onwards on its next `tar_make()` (needs the `cutadaptenv` conda env). `tar_prune()` results: `store_dada2` 208 → 37 meta rows, `store_assign_taxo` 509 → 108, `store_cross_val` 138 → 82.

- [x] **S0.1** `[Critical / easy]` `mini_db` is `TRUE` in `config.R` and the store cannot tell mini from full runs. Read it from `BENCHMARK_MINI_DB` (default FALSE), print the flag + store path at pipeline start, and route smoke runs to `store_*_mini` (see S3.1). → [01 §B2](critique_fable/01_bugs_and_traps.md)
- [x] **S0.2** `[High / easy]` Create `figures/` and `dir.create()` it in the notebook setup; 26 `ggsave()` calls currently abort the render (ggplot2 4.0.3, `create.dir = FALSE`). → [01 §B3](critique_fable/01_bugs_and_traps.md)
- [x] **S0.3** `[High / easy]` `script_dada2.R`: fix `file_refseq_taxo` (`refseq/dada2_format/Unite_Fungi.fasta`), replace the dead `~/Nextcloud/IdEst/Projets/MiscMetabar/R/` source with the pqverse loader (S2.2), fix the doubled `paste(getwd(), here(...))` output paths. Required before any D1b-A / D1c DADA2 rerun. → [01 §B4](critique_fable/01_bugs_and_traps.md)
- [x] **S0.4** `[Medium / easy]` Fix the contradictory comments in `config.R` (`cv_fold_tested`, header about `script_dada2.R`) and `make.R` (step 3). → [01 §B5](critique_fable/01_bugs_and_traps.md), [05 drift table](critique_fable/05_analysis_and_docs.md)
- [x] **S0.5** `[Medium / easy]` `git rm --cached v2.31.0.tar.gz` (vsearch source tarball) and ignore it; name and move `41576_2023_679_MOESM2_ESM.ods` to `docs/references/`; move `design_analysis.svg` to `docs/`. → [03](critique_fable/03_layout_and_files.md)
- [x] **S0.6** `[Medium / easy]` Delete `.RData` (31 MB), `.Rhistory`, `renv/` (539 MB); set `RestoreWorkspace: No` / `SaveWorkspace: No` in the `.Rproj`; add `.claude/settings.local.json` to `.gitignore`. → [01 §B9](critique_fable/01_bugs_and_traps.md), [03](critique_fable/03_layout_and_files.md)
- [x] **S0.7** `[Medium / easy]` `tar_prune()` the three stores (509 meta rows in `store_assign_taxo`, three pipeline generations mixed) and add `tar_prune()` after each `tar_make()` in `make.R`. → [01 §B8](critique_fable/01_bugs_and_traps.md)

### Phase 1 — Correctness of the cost layer (M2)

- [ ] **S1.6** `[High / moderate]` **Two autometric logs of the same crew worker overlap in time, so `peak_resident_mb` is attributed to the wrong phase** (found 2026-09-22, on the OOM-killed production run). Evidence: `otu_compute_dada2__Unite_s_all_20250219_Fungi__20260922_115819.txt` (994 samples, 11:58:19 → 12:14:54) and `otu_compute_dada2__EUK_ITS_v2.1__20260922_120413.txt` (640 samples, 12:04:13 → 12:14:55) carry **the same pid (194100)**, overlap for 640 s and end at the same instant, the OOM kill. The first file's trajectory rises to 11.7 GB, drops to 1.8 GB at 12:04:19 — the moment the second phase starts — then climbs to 49 GB, which is the *second* computation's memory written into the first one's file. `with_autometric()` does call `autometric::log_stop()` through `on.exit()`, so either the stop does not take effect before the next `log_start()`, or the first target had not really finished (its `progress` row says "completed" but no meta row was written, and the rerun of 2026-09-22 re-dispatched it). Diagnose before trusting any per-phase memory figure. **Diagnosed and the aggregation fixed 2026-09-22.** The hypothesis above was wrong on its central point: the samples are never mislabelled. `phase` is correct in every row of both files — file 1 holds 353 rows of its own phase and **641 rows labelled `otu_compute_dada2__EUK_ITS_v2.1`**, file 2 holds 640 rows of that same phase — because `autometric::log_phase_set()` is process-global, so a leaked logger keeps writing under whatever phase the worker sets next. The file *name* is the only thing that lies, and the data was therefore fully recoverable without a rerun. Two behaviours pinned down on autometric 0.1.2: a `log_start()` issued while a logger already runs is **silently ignored** (the second file is never created), and a clean `log_stop()` → `log_start()` sequence is correct, so the production interleaving needs an interruption that skips `with_autometric()`'s `on.exit()` — consistent with the OOM kill, not reproduced here. **Two defects found in `summarise_autometric_costs()`, both independent of the logger.** First, `dplyr::filter(log_file == max(log_file))` selected a phase's run by the **alphabetical** maximum of file names that begin with the phase, not by time, so a phase whose rows had leaked elsewhere could be summarised from the wrong file — or from a stale run. Second, and only visible once the first was fixed, **`autometric::log_read()` returns `time` in seconds since the first sample of its own file, not an epoch** (checked: raw field 6 is 1790076910.453 = 13:35:10, `log_read()` gives 0), so times from two files are not comparable; a first attempt at unioning rows by time overlap merged unrelated runs and inflated `compute_dada2__EUK_ITS_v2.1` from a true 1668 s to 2629 s. Fixed by reconstructing `abs_time = run_started + time`, `run_started` parsed from the `__YYYYmmdd_HHMMSS` suffix (mtime fallback), then selecting per phase the file with the latest `run_started` and adding the rows of that same phase from any file overlapping it **on the shared clock**. Correction measured over the 842 log files (711 non-empty, 64 381 rows, 330 phases, **95 files holding more than one phase**): 20 phases had a wrong `peak_resident_mb`, 9 understated and 11 overstated. The understatements are the ones that matter, because they are the heavy computations: `otu_compute_dada2__EUK_ITS_v2.1_Fungi_rep` 1501.8 → 23 830.1 MB (15.9×), `compute_dada2__Unite_all_20250219` 3078.1 → 23 010.3 MB (7.5×), `otu_compute_dada2__Unite_all_20250219_Fungi_rep` 2256.9 → 14 212.8 MB (6.3×). The overstatements are all cheap derive rows losing a neighbour's peak (`blastn__Unite_all_20250219___0.5...rel_majority` 2012 → 1254 MB). **The item's claim that wall time and CPU are unaffected was wrong**: 50 of 330 phases had a wrong `wall_time_s`. (An intermediate version of this fix, which compared the per-file relative `time` as if it were an epoch, reported 89 and 75 instead of 20 and 50; those counts were an artefact of that bug and are recorded here only so the discrepancy is not rediscovered.) Verified against an independent source, `targets::tar_meta()$seconds`, on the 131 phases that are targets: median absolute error 2.4 s under both implementations, but phases off by more than 10 s fall from 26 to 21 overall and, on the targets that ran more than 60 s, from **8 to 3**; the three catastrophic cases now agree with targets to about 1 % (`otu_compute_dada2__EUK_ITS_v2.1_Fungi_rep` targets 666.2 s, old 3.0, new 663.9; `compute_dada2__Unite_all_20250219` 410.2 / 47.0 / 408.1; `compute_dada2__EUK_ITS_v2.1` 1668.5 / 2004.5 / 1665.9). The 3 remaining outliers are 2–20 % **undershoots** identical in both implementations (`compute_lca__Unite_all_20250219_Fungi_rep` 82.1 against 102.3), i.e. target overhead — reference loading, serialisation — that falls outside the logged window, not a misattribution. The 35 sub-second targets keep a ~31 s median error in both implementations, an artefact of the phase label persisting between targets; they never reach the figures, since `benchmark_costs` right-joins `values_map` on `compute_name` and the derive rows inherit their computation's cost. `benchmark_costs` rebuilt the same day from the corrected logs: 504 rows, **0 NA**, peak 408 MB (minimum) / 1512 (median) / **58 105 (maximum, `compute_dada2__EUK_ITS_v2.1`)**, which is also the number S8.6's ≥ 45 GB CV budget should be read against. Chapter 05 re-rendered. **Deliberately not applied** (developer, 2026-09-22): the leak guard in `with_autometric()`, an `autometric::log_stop()` immediately before `log_start()`, because it invalidates 604 targets — all 96 computations, a 3 h 32 re-run — where the aggregation fix invalidated `benchmark_costs` alone (measured with `tar_outdated()` both ways). It lands with the next production run. Evidence scripts kept in the session scratchpad only; the reproducible part is `read_autometric_dir()` / `summarise_autometric_costs()` themselves.
  **What this invalidates:** the per-phase peak memory of `benchmark_costs` whenever a worker ran two targets back to back — which is most of them, one worker per controller. The wall-time and CPU columns are unaffected (they come from the sample timestamps of the phase's own file, and the overlap only adds samples at the end). This compounds S1.1, which already said the per-database split was unusable because runs are mixed; the two together mean **no per-database or per-phase memory figure is publishable until a clean run is measured with this fixed**.
  **What it does not invalidate:** the totals, and the fact that dada2 against `EUK_ITS_v2.1` (1 877 003 records) passes 48 GB and was OOM-killed on a 62 GB machine. Memory does scale with the reference: `Unite_s_all_20250219_Fungi` (168 030 records) was at 11.7 GB when it handed over.

Applied 2026-09-10 (`R/autometric_helpers.R`, logs under `data/data_final/autometric/<pipeline>/`, `d_asv_file` target). Not yet verified on a real run: the M2 figures stay open until the production rerun regenerates `benchmark_costs` (see M2 re-validation in section 2).

- [x] **S1.1** `[Critical / moderate]` `benchmark_costs` is built from the 2025-02-11 *sequential* run: crew workers never log a phase (no `log_start()` in the worker) and the autometric log is append-only across runs. Start a per-target logger inside the worker (`with_autometric()` helper, one file per target per run), aggregate from the directory, then **rerun and regenerate M2.1, M2.2, M2.3, Q2.5**. Those four items are ticked in the done log but must be considered open until this lands. → [01 §B1](critique_fable/01_bugs_and_traps.md)
- [x] **S1.2** `[High / easy]` Make `d_asv` a `format = "file"` dependency of `store_assign_taxo` (`store_dada2/objects/d_asv`) so a rebuilt DADA2 store invalidates the assignments. → [01 §B7](critique_fable/01_bugs_and_traps.md)
- [x] **S1.3** `[High / easy]` All 18 blastn targets of the current (mini) store added no taxonomy column: `assign_blastn()` returns the input unchanged with only a message when no hit passes the score filters. Guard applied 2026-09-10: `combine_taxo_assignments()` now stops and names empty assignments. Open: confirm on the production rerun that blastn hits with the full databases, and propose a loud warning upstream in MiscMetabar. → [01 §B11](critique_fable/01_bugs_and_traps.md) Update 2026-09-10: the 2026-05 store ran every target on `mini_Unite` (S6.1), which explains most missing hits; on the 2026-09-10 mini run blastn added its columns (3 of 400 genera, same reference bug). Re-check after S6.5. **Cause found 2026-09-11**: `assign_blastn()` defaults to `min_cover = 95` (BLAST `qcovs`), which neither `pipelines/assign_taxo.R` nor `cross_val()` overrides. The ITS1F–ITS2 amplicon extends beyond the ITS references: median best query cover 84 % on `mini_Unite_s_all_20250219_Fungi`, 100 % on the primer-trimmed `mini_EUK_ITS_v2.1_Fungi_cut`. Of the 71 mock ASVs with a UNITE hit at ≥ 95 % identity, bit score ≥ 50 and e-value ≤ 1e-30, only 1 passes cover ≥ 95 % (7 at ≥ 90 %, 51 at ≥ 80 %, 64 at ≥ 50 %). Same defaults in CV: blastn reaches 0.06 good classifications on `cross_val_mini`, against ≈ 0.6 for the other methods. Every blastn result so far measures this filter, not the method. Decision 18 (2026-09-11): keep `min_cover = 95`; the low hit count is attributed to the `mini_*` databases. Full-database diagnostic (2026-09-11, same 200 mock ASVs): every ASV has a hit at ≥ 95 % identity, bit score ≥ 50 and e-value ≤ 1e-30 on the uncut databases, but only 44 keep one at cover ≥ 95 % on `Unite_s_all_20250219_Fungi` (median best cover 88 %) and 88 on `EUK_ITS_v2.1_Fungi` (93 %); 84 of 123 on the primer-trimmed `EUK_ITS_v2.1_Fungi_cut` (100 %). The mini databases explain the missing hits; the cover filter still removes most of them on the uncut databases. Cause confirmed: the unaligned part is at the start of the ASV (156 of 200 best hits below 95 %, median 32 bp unaligned, reference aligned from position 1). The ASVs keep 30–45 bp of the 18S end (`…GGATCATTA` at median position 37 in 196 of 200 ASVs) that ITS references do not contain, so a 95 % cover is out of reach for most ASVs whatever the database size. → [07 §B18](critique_fable/07_results_validity.md) Decision 18 revised: `min_cover = 80` (`config.R::blastn_min_cover`, `min_cover` column of the blastn rows of `values_map` and `cv_values_map`, NA elsewhere, so a new value reruns the blastn targets only). End-to-end check with `add_new_taxonomy_pq()` on the full `Unite_s_all_20250219_Fungi`: 44 of 200 mock ASVs get a genus at cover 95, 180 at cover 80. Open: confirm on the mini and production reruns that blastn now assigns the mock ASVs, and propose a louder warning upstream in MiscMetabar. Production `assign_taxo` of 2026-09-15 (cover 80): blastn (`rel_majority`, raw ASVs) gives a genus to 90.8–99.5 % of the mock ASVs on every database, 99.5 % on `EUK_ITS_v2.1_Fungi_cut`; the missing hits of the mini runs came from the mini databases. Open: the MiscMetabar warning, and the high NA share of blastn in standard cross-validation: on the production run of 2026-09-15 blastn leaves 87–95 % of the queries without genus in the standard variant (0.04–0.12 good, ≤ 3 % wrong), against 1–3 % NA in the leaked variant, while lca (same vsearch hits) leaves 1–5 %. **Dominant cause found 2026-09-16, and it is not the filters** (B24): `cross_val()` reduces the reference to the drawn pool, so blastn searches a 5 413-record database, not the 156 820 of `Unite_all_20250219`. Outside the pipeline, the same 200 queries and the same filters give 58.5 % assigned against the full database and 9.0 % against a random 5 413-record subsample (26.5 % at identity 90), while the pipeline reaches 2 %: the reference size carries most of the drop, and a residual gap of 9 % against 2 % stays open. **Investigation closed 2026-09-16**: running the pipeline path itself against the **full** `EUK_ITS_v2.1`, with `reduce_reference = FALSE` and the production filters, blastn leaves **19.2 % NA** and reaches 0.737 good genus. The 87–95 % NA of the 2026-09-15 run were therefore an artefact of the reduced reference from end to end, and no defect remains in `assign_blastn()`, `blast_pq()` or the vote. The `min_id = 95` default still costs points and stays a Methods choice, not a bug. Measurements on 200 trimmed queries of that database with the query records removed: against the **full** database, 85 % of the queries keep a hit at identity ≥ 90 and cover ≥ 80 (median best identity 96.3 %, median cover 100 %), and 58.5 % at the `assign_blastn()` default of identity ≥ 95; the pipeline path on one fold assigns 2 % (`prop_NA` 0.98). `assign_blastn()` is innocent: called on 50 of the same queries against the full database it gives a genus to 29 (58 %) and its names match the phyloseq taxa 30 of 30; `blast_pq()` asks BLAST for `qcovs`, so the cover is the query cover; `resolve_vector_ranks(method = "rel_majority")` assigns on a plurality (`A,A,B,C,D` → `A`) and returns NA only on a tie. The CV also leaves `min_id` at the `assign_blastn()` default of 95 (only `min_cover` is passed), which costs about 25 points on the full database but is secondary to the reference size. **Closed 2026-09-22 on the production `assign_taxo` run of the nine-database grid** (`store_assign_taxo`, 12:26–15:58, 587 targets, 0 error, 0 warning): measured on the 195 mock ASVs of `d_all_taxo` alone, the 97 `fake_` and 100 `external_` controls counted apart (C22-P8), raw-ASV blastn at `min_cover = 80` names a kingdom for 89.2–100 % of them on every one of the 9 databases (lowest cell `Unite_s_all_20250219`, highest 1.000 on the three EUKARYOME databases) and a genus for at least 86.2 % with the `rel_majority` vote, mean over the 9 databases 0.983 / 0.959 / 0.945 / 0.904 at `min_id` 90 / 92 / 95 / 97. **No blastn column is empty: 0 of 756.** The low genus cells belong to the `unanimity` vote (0.251–0.548, it abstains on any disagreement by design) and to `abs_majority` (0.810–0.884), not to the cover filter, which is what S1.3 asked. Control side, same columns at Genus: blastn names **0 %** of the shuffled `fake_` controls, the best abstention of the eight methods (dada2 0.109, lca 0.113, sintax 0.208, itsx_sintax 0.287, itsx_lca 0.338), and 31.6 % of the non-fungal `external_` controls against 0.649 (sintax), 0.681 (dada2) and 0.931 (lca) — blastn is the conservative method on both control classes, and the filters that once removed everything now remove the right things. The confirmation half is therefore done; the second half, the loud warning upstream in MiscMetabar when no hit passes the filters, is a written proposal and moved to **S6.6** with the other upstream reports.

### Phase 2 — One source of truth for parameters and dependencies

Applied 2026-09-10. `tests/test_values_map.R` proves the new builders reproduce the legacy target names byte for byte, so no store object was orphaned by S2.1. Behaviour change to know about: crew workers now run the **dev checkout** of MiscMetabar / comparpq (`load_pqverse()` inside each target); before, they silently ran the installed CRAN MiscMetabar because `library("MiscMetabar")` put it in `tar_option_get("packages")`. `d_asv_for_assignation_fake` renamed `d_asv_shuffled` (invalidates the assignments, already forced by S0.1). The Q2.1 `db_meta` join was silently NA for every EUK database (v1_9_3 names); fixed by moving `db_meta` to `config.R`.

- [x] **S2.1** `[High / easy]` `R/values_map.R` with `build_methods_grid()`, `build_values_map()`, `build_cv_values_map()`, `full_name_for()`; `db_list` and `db_meta` move to `config.R`; delete `R/values_map_for_qmd.R`; add `tests/test_values_map.R` pinning the expected target names. → [04](critique_fable/04_pipeline_code.md)
- [x] **S2.2** `[High / easy]` `R/load_pqverse.R` + `PQVERSE_PKG_DIR` env var replacing the 15 absolute paths (plus one dead path in `script_dada2.R`) and the file-by-file `source()` of comparpq; drop `library("MiscMetabar")` where `load_all()` follows; capture `session_info()` as a target. → [02](critique_fable/02_dependencies_and_paths.md)
- [x] **S2.3** `[Medium / easy]` Rename `R/functions.R` → `R/create_fake_pq_from_refseq.R`; `Phyla` → `Phylum` in `script_dada2.R`; `d_asv_for_assignation_fake` → `d_asv_shuffled`; pass `targets_seed` to `derive_fake_ref()`; `tempfile()` instead of the fixed `test_refseq.fasta` in `cross_val()`. → [01 §B6](critique_fable/01_bugs_and_traps.md), [04](critique_fable/04_pipeline_code.md)

### Phase 3 — Layout

Applied 2026-09-10. Scripts moved to `pipelines/{dada2,assign_taxo,cross_val}.R`; `_targets.yaml` defines five projects (`TAR_PROJECT`), the `*_mini` ones own their stores and set `mini_db` (the `BENCHMARK_MINI_DB` variable of S0.1 is gone). `analysis/` is a Quarto project: `00_setup.R` + chapters `01_load_and_clean` → `06_cross_validation`, `sandbox/` for the unfinished material, `_cache/` for the inter-chapter objects; `benchmark.qmd` deleted. Stale `v1_9_3` names found and fixed on the way: `preference_pattern` (the preference consensus matched no column), Q2.2 pairs, Q3.5 preferred cell, the `tc_bar`/`tc_circle` exploratory calls. `data/README.md` documents every subfolder. `here::i_am()` is required in each chapter because `_quarto.yml` is itself a `here` root criterion.

- [x] **S3.1** `[Medium / moderate]` `_targets.yaml` with named projects (`dada2`, `assign_taxo`, `assign_taxo_mini`, `cross_val`, `cross_val_mini`); `make.R` becomes three `run_project()` calls; optionally move the three scripts to `pipelines/` and drop the `_parallel` suffix. → [03](critique_fable/03_layout_and_files.md)
- [x] **S3.2** `[Medium / easy]` Move `In_silico_simulation.qmd` to `analysis/`; write `data/README.md` describing every `data/` subfolder, including the undocumented `mock_hleap2021/`, `taxo_mock_SRR30413326.csv` and the empty `rawseq/SRR30413326/` (keep or delete?). → [01 §B10](critique_fable/01_bugs_and_traps.md), [03](critique_fable/03_layout_and_files.md)
- [x] **S3.3** `[Medium / moderate]` Turn `analysis/` into a Quarto project (`_quarto.yml`, `execute-dir: project`, `freeze: auto`) and split `benchmark.qmd` (1 381 lines) into `00_setup.R` + numbered chapters (load/clean, Q1, Q2, Q3, M2, CV) + `sandbox/` for lines 1077–1381; add a `save_fig()` helper. → [05](critique_fable/05_analysis_and_docs.md)

### Phase 4 — Tests and documentation

Applied 2026-09-10. `tests/run_all.R` runs six files (65 expectations). The new `derive_fake_ref()` test exposed a real bug (`sample()` with `n` larger than the pool, and the length-one `sample(x)` trap), fixed in `make_databases.R`. CLAUDE.md was rewritten from scratch (describes what is on `make.R`'s path today), CONTEXT.md and README.md updated; the drift table of critique 05 is fully applied. Remaining doc debt is tracked as it appears, not here.

- [x] **S4.1** `[Medium / easy]` `tests/run_all.R` (`testthat::test_dir`) and tests for `cv_to_tidy()`, `create_fake_pq_from_refseq()`, the awk/sed helpers of `make_databases.R`, and the cost aggregation (two-runs-same-phase fixture). → [04 §Tests](critique_fable/04_pipeline_code.md)
- [x] **S4.2** `[High / easy]` Apply the documentation-drift table (README, CLAUDE.md, CONTEXT.md, `config.R`, `make.R`, `proposals_for_dbpq.md`): six DBs not nine, parallel script is the only one, `make.R` runs everything, CV knobs at publication values, `mini_db` documented, v2 names. Rule: CLAUDE.md describes what is on `make.R`'s path today; history goes to `Archives/README.md`. → [05 §Documentation drift](critique_fable/05_analysis_and_docs.md)
- [x] **S4.3** `[Low / easy]` Align the project name: README title `benchmark_taxo_assign` vs folder/Rproj `benchmar_assign_taxo`. Keep the folder (Nextcloud + memory paths), fix the title. → [03](critique_fable/03_layout_and_files.md)

### Phase 5 — tidypq adoption (decision 2026-09-10, see decisions 8–13)

S5.1 applied 2026-09-10 in `01_load_and_clean.qmd` (`tax_table_to_df()` + one `mutate_taxa_pq()` call for the `Gen_sp_*` columns, NA proportions from the tibble) and in `sandbox/distance_to_true_community.qmd` (`pq_to_tidy()`; note it reports unassigned ranks as `"Unknown"` and drops zero abundances). Each rewrite was checked against the 2026-05 store: identical values and taxa order. The fake-taxa proportion keeps `grepl()` on `taxa_names()`, clearer than a `filter_taxa_pq()` call there. S5.3 done through the CLAUDE.md / CONTEXT.md rewrite.

S5.2 applied 2026-09-10, narrower than planned. `combine_taxo_assignments()` now uses `tax_table_to_df()` + one `mutate_taxa_pq()` call and aligns rows on the taxon name instead of assuming the same order; rebuilding `d_all_taxo` from the 72 stored mini assignments gives the stored object exactly (529 columns, same taxa order, same values). The `tax_table` extractions of `cross_val()` use `tax_table_to_df()` and `phyloseq::rank_names()`; tidypq is loaded in the `cross_val` workers. **Not replaced**, contrary to the first inventory: the sample renaming of `pipelines/dada2.R` and `create_fake_pq_from_refseq()`, because `rename_samples_pq()` / `rename_taxa_pq()` rename *columns*, not sample or taxa names (critique 06 corrected).

`tidypq` (`pqverse_pkg/tidypq`) now provides verbs at four scales (samples, taxa, occurrences, tree) plus `pq_to_tidy()` and `tax_table_to_df()`. The workspace rule is: prefer `tidypq::pq_to_tidy()` over `psmelt()` and never hand-roll the phyloseq-to-tibble pipeline. Call-site inventory and verb mapping: [06](critique_fable/06_tidypq_adoption.md).

- [x] **S5.1** `[High / moderate]` Notebook: rewrite the `@tax_table` slot manipulations of `analysis/benchmark.qmd` (lines 58–103, 172–207, 247) with `tax_table_to_df()` / `mutate_taxa_pq()` / `rename_taxa_pq()`, the fake-taxa filters with `filter_taxa_pq()`, and the `psmelt()` at line 1090 with `pq_to_tidy()`. Do it during the S3.3 split so each chapter is rewritten once. Add `tidypq` to the loader (S2.2). → [06](critique_fable/06_tidypq_adoption.md)
- [x] **S5.2** `[Medium / moderate]` Pipelines: `combine_taxo_assignments()` on `tax_table_to_df()` + `mutate_taxa_pq()` (rows aligned on the taxon name); the `tax_tib` extractions in `cross_val()` on `tax_table_to_df()`. The dada2 sample renaming and `create_fake_pq_from_refseq()` stay as they are (no matching verb). → [06](critique_fable/06_tidypq_adoption.md)
- [x] **S5.3** `[Low / easy]` Update CLAUDE.md "External code dependencies" (tidypq is now a dependency) and CONTEXT.md (`pq_to_tidy` entry).

### Phase 6 — Validity of the stored results (found 2026-09-10)

Evidence, commands and validation plan: [07](critique_fable/07_results_validity.md). Found while validating the restructured pipelines on an `assign_taxo_mini` run. These defects change scientific results: **no figure or table computed from `store_assign_taxo` or `store_cross_val` since 2026-05-26 can be used**. The Q1, Q2, Q3, M2 and D1a items of the done log are reopened by S6.5.

- [x] **S6.1** `[Critical / easy]` Every assignment and cross-validation target used the first database: `ifelse(mini_db, ...)` on a scalar recycles its first value (commit `17ecc9e`, 2026-05-26). Fixed in `R/values_map.R`; `tests/test_values_map.R` now requires one reference file per database, and the three plans now use 12 distinct reference files. → [07 §B12](critique_fable/07_results_validity.md)
- [x] **S6.2** `[Critical / easy]` UNITE sintax conversion drops the kingdom (`…|k__X;tax=p:…` in all 266 589 / 168 049 records of `Unite` / `Unite_Fungi`), so sintax / lca / blastn ranks are shifted by one (mock F1 = 0 for those methods on the mini run). `derive_sintax()` now rewrites `|k__` into `;k__` before `dbpq::format2sintax()`; tested. → [07 §B13](critique_fable/07_results_validity.md)
- [x] **S6.3** `[Critical / easy]` ~~Regenerate the four UNITE sintax files in place.~~ Superseded on 2026-09-11 by S7.1: every reference file is rebuilt from the general FASTA releases under versioned names; the old files stay on disk as legacy.
- [x] **S6.4** `[High / easy]` Cross-validation: empty truth table for dada2-format headers (dada2 = 0 for every database and rank) and lca ranks named `<rank>_sintax`. Fixed in `create_fake_pq_from_refseq()` and `cross_val()`, tested; chapter 06 normalises stores built before the fix. → [07 §B14–B15](critique_fable/07_results_validity.md)
- [ ] **S6.5** `[Critical / moderate]` After S7.4: `assign_taxo_mini` and `cross_val_mini` runs with the checks listed in 07, then production `dada2` → `assign_taxo` → `cross_val`, then re-render every chapter. Re-tick the done-log items only then. Mini part checked 2026-09-11 on both stores: 0 error, 0 warning; Kingdom = Fungi for ≥ 97 % of the ASVs assigned by dada2, sintax and lca on every database; `Genus_lca` differs between databases (7 distinct columns for 8 databases); CV ranks are plain names and dada2 reaches 0.61 good classifications. **Blocking before production: blastn assigns almost nothing (S1.3).** Analysis part checked 2026-09-14 on the mini stores of the compute/derive + ITSx pipeline (288 and 82 targets, 0 error, 0 warning): chapters 01–06 render with `BENCHMARK_ANALYSIS_MINI=TRUE` without error (14 figures in `figures/mini`). Two fixes in `01_load_and_clean.qmd`: `ranks-df` drops empty assignments on the mini stores only (12 blastn rows, the guard still stops in production), and `explore-tc-bar` skips when a rank column has no assignment (`psmelt()` drops all-NA columns). `res_comp_tax` has 192 `method_db`: 180 assignment columns (192 grid rows − 12 empty blastn rows on mini) + 12 consensus. `filter_default_settings()` keeps the four methods without ITSx only (91 metric × rank rows per `method_db`), so the Q1–Q3 figures do not mix the two inputs; blastn is missing there on 3 mini databases (`EUK_ITS_v2.1`, `EUK_ITS_v2.1_Fungi`, `EUK_SSU_v2.1_Fungi_cut`). Warning-visible render (`-M warning:true -M message:true`) after the S6.8 fixes: 0 error in 01–06, 209 MCC `NA` (all zero-margin), no overflow, shape-palette or pdf-glyph warning left; only ggplot "Removed N rows" warnings from the `NA` metrics of the mini databases. Production runs done 2026-09-14/15 (`dada2`, `assign_taxo`) and 2026-09-15 16:03–16:50 (`cross_val`, after the B23 fix), all with 0 error and 0 warning. Chapters 01–06 re-rendered on the production stores on 2026-09-16 (07:53–07:58, 0 error, 14 figures in `figures/`). Chapter 01 needs 2 min 30 and peaks at 1.4 GB of memory (measured outside Quarto), so the memory kill seen during a first attempt came from the machine, not from the chapter. `res_comp_tax` holds 144 `method_db` (132 assignments + 12 consensus), `res_comp_tax_otu` 132, and `d_otu_taxo` 340 taxa (143 OTUs + the 197 controls). 86 of the 13 104 metric values are NA, all from a zero margin: 55 `TNR` at Kingdom, 31 `FDR` / `PPV` / `TAR` spread over the other ranks (the same value repeated, since TAR is an alias of PPV). Headline mock result at Genus (F1): consensus columns 0.815 on average (12 columns, max 0.859), single assignments 0.782 (132 columns, max 0.859 for `dada2__Unite_s_all_20250219_Fungi___0.5`); by method, `itsx_dada2` 0.831, `itsx_sintax` 0.822, `sintax` 0.815, `lca` 0.800, `blastn` 0.757, `dada2` 0.716. Open: re-tick the done-log items, and the remaining figures (Q4, Q5).
- [ ] **S6.6** `[Medium / easy]` Upstream reports: dbpq `format2sintax()` on UNITE general-release headers; MiscMetabar `assign_sintax()` / `assign_vsearch_lca()` / `assign_blastn()` parse predictions by position, so a missing rank silently shifts every column. Also (2026-09-11): `assign_sintax()` / `assign_vsearch_lca()` drop empty taxa from their queries by default (`clean_pq = TRUE`), which silently skipped the negative controls (B19). Also (2026-09-14): comparpq `tc_bar()` goes through `psmelt()`, which drops all-NA rank columns, so a rank with no assignment fails with "Can't subset elements that don't exist" instead of a clear message (worked around in `01_load_and_clean.qmd` `explore-tc-bar`).
- [x] **S6.7** `[Critical / easy]` sintax and lca never queried the negative controls (`clean_pq = TRUE` removes taxa without reads, and the `fake_*` / `external_*` controls have none): their TN, TNR, MCC and ACC were overstated in every store. Fixed 2026-09-11 in `R/assign_compute.R` (`clean_pq = FALSE`). → [07 §B19](critique_fable/07_results_validity.md)
- [x] **S6.8** `[Critical / easy]` MCC lost to integer overflow in `comparpq::tc_metrics_mock_vec()`: 14 of 1344 MCC values `NA` on the mini store, the most balanced confusion matrices. Fixed 2026-09-14 in the comparpq dev checkout (double precision, regression test, NEWS). Zero-margin convention decided 2026-09-14 (developer): MCC = 0 (Chicco & Jurman 2020), in `tc_metrics_mock_vec()` itself (comparpq test + NEWS); on the mini store it concerns 209 of 1344 cells: 120 ranks where the method assigns nothing (TP + FP = 0), 80 Kingdom cells where every control gets a kingdom (TN + FN = 0), 9 Phylum cells (FP + TN = 0). Checked 2026-09-14 by a warning-visible render of chapters 01–06 on the mini stores (with the 156-row `values_map` of S8.5): 0 error, 0 `NA` MCC (167 zero MCC of 1092), only ggplot "Removed N rows" warnings left. Also found by the warning-visible render: M2 figures lost 2 of 8 databases (`shape = db`, ggplot palette of 6 shapes) and the Q2 pdf figures replaced `→` / `−`; both fixed in `05_m2_costs.qmd` (`db_shapes`) and `save_fig()` (`cairo_pdf`). M2.2 also estimated every CO₂eq with `n_cores = n_threads` (4) although the computations run on `assign_threads_dada2` (3) or `assign_threads_fast` (1) threads since S8.3; fixed per row from `method`. Render check for the chapters: `quarto render <chapter> -M warning:true -M message:true` (`-M execute:warning:true` has no effect). → [07 §B20](critique_fable/07_results_validity.md)
- [ ] **S6.9** `[Critical / easy]` The two `_Fungi_cut` databases are not trimmed to the amplicon region (found 2026-09-15, from the developer's remark that cutadapt loses too many records). `dbpq::cutadapt_rm_primers_db()` builds the linked adapter `-a '<primer_fw>...<primer_rev>'` with the reverse primer as written, not its reverse complement (MiscMetabar `cutadapt_remove_primers()` uses `dada2::rc()`), and adds `--discard-untrimmed`. With cutadapt 2.6 (`cutadaptenv`) a record counts as trimmed as soon as one of the two adapters matches (checked on synthetic records), so the 3′ side is never trimmed and every record without the ITS1F site (18S end) is dropped. Measured: `EUK_ITS_v2.1_Fungi_cut` keeps 51 602 of 1 076 870 records (4.8 %), 86.9 % of them still hold the ITS2 site, and their median length (558 bp) exceeds that of `_Fungi` (523 bp): the survivors are the records that start in the 18S. `EUK_SSU_v2.1_Fungi_cut` keeps 116 746 of 141 884 records, each reduced to the 18S tail downstream of ITS1F (median 46 bp, 45–47 bp for 5–95 %). On 3000 random records, ITS1F is found in 0.1 % (`Unite_s_all_20250219_Fungi`) and 2.0 % (`EUK_ITS_v2.1_Fungi`), the reverse complement of ITS2 in 94.5 % and 93.7 % (median position 214 and 210 bp), the reverse primer as written in 1 of 3000. Every Q2 result on `_cut`, the B18 cover figure on `EUK_ITS_v2.1_Fungi_cut` and the S8.4 genus-retention figures for `_cut` come from these files. Fix proposed, not applied (methods choice): 3′ trim at the reverse complement of ITS2, 5′ trim at ITS1F when present, keep the records without the 5′ site (≈ 94 % of the records kept, ≈ 210 bp: ITS1 + start of 5.8S), and a rule for the ≈ 6 % without the ITS2 site; in dbpq (reverse complement, explicit required / optional adapters) or at the `derive_cutadapted()` call site. Decision 7 must be revisited: ITS1 is not part of the 18S gene, so an SSU record holds at most the 18S tail the ASVs start with (reverse complement of ITS2 in 0.6 % of SSU records). **Code fixed 2026-09-15** (developer decisions 7 revised, 23): dbpq `cutadapt_rm_primers_db()` reverse-complements `primer_rev` (`rc_primer_rev = TRUE`, `Biostrings::reverseComplement()`), marks each primer `;required` / `;optional` (`primer_fw_required`, `primer_rev_required`) and takes `min_overlap` (`-O`); tests and NEWS (breaking change) in the dbpq checkout. `derive_cutadapted()` gains `discard_untrimmed` (dbpq default `TRUE`), set to `config.R::cut_discard_untrimmed = FALSE` here, with `min_overlap = config.R::primer_min_overlap` (20, the ITS2 length) and `nproc = n_threads`. Checked on 3000 random records: every record kept, 94.1 % trimmed on `EUK_ITS_v2.1_Fungi` (median 524 → 211 bp), 95.4 % on `Unite_s_all_20250219_Fungi` (523 → 216 bp). SSU removed from `config.R` (source and database): `db_list` has 7 databases, `itsx_db_list` 4, `values_map` 132 rows and 44 computations, the CV grid 56 targets; the SSU files stay on disk. Rebuilt 2026-09-15 at 12:19 (developer): 1 076 870 records in both formats, same headers in the same order as `_Fungi`, 94.8 % trimmed, median 523 → 211 bp, `mini_*` = the first 10 000 records. 317 records are empty and 1530 shorter than 50 bp: ITS2-only records that start at the 5.8S site, so nothing is left before the reverse-primer site (none in the `mini_*` files). Checked on a 503-record reference holding 3 empty records: vsearch (sintax, usearch_global) discards records shorter than 32 bp (`minseqlength`), makeblastdb and dada2 `assignTaxonomy()` run without error, so dada2 and blastn keep these 1847 short references and sintax / lca do not (0.17 % of the database, no ITS1 to match). `tar_outdated()` on `assign_taxo`: 38 targets (8 computations, their 24 rows, 2 reference files, `d_all_taxo`, `otu_clusters`, `benchmark_costs`, `session_info`). Decided the same day (developer): drop the short records from the `_cut` databases. dbpq `cutadapt_rm_primers_db()` gains `min_length` (cutadapt `-m`), passed by `derive_all_variants()` as `config.R::cut_min_length = 50` (the query length filter of `cross_val()`), so every method reads the same references. The mini runs launched at 12:25 were stopped. Rebuilt again by the developer the same afternoon: 1 075 023 records, minimum 50 bp, 5.2 % kept whole without primer site (median 642 bp), trimmed records median 208 bp; the `mini_*` file holds 732 untrimmed records of 10 000. `assign_taxo_mini` (207 targets): 0 error, 0 warning, no SSU column, `otu_clusters` 195 ASVs in 143 clusters (29 with more than one ASV); on `mini_EUK_ITS_v2.1_Fungi_cut` the raw blastn assignments added no column and `itsx_blastn` gives a genus to 1 % of the mock ASVs (mini database, S1.3), dada2 92 %, sintax 50 %, lca 100 % (0.5 threshold). `cross_val_mini` stopped on `cv__sintax__EUK_ITS_v2.1_Fungi_cut__standard`: the `_cut` records no longer hold the ITS2 site, so `trim_cv_queries()` keeps no query (gap in decision 24, D1a). CV rule for the `_cut` database decided and implemented (D1a). Production `assign_taxo` rerun by the developer on 2026-09-15 (0 error, 0 warning): on `EUK_ITS_v2.1_Fungi_cut` (raw ASVs, 0.5) dada2 gives a genus to 95.9 % of the mock ASVs, sintax 96.9 %, lca 63.6 %, blastn 99.5 %. Open: production `cross_val` (S8.6). → [07 §B21](critique_fable/07_results_validity.md)

### Phase 7 — Reference database provenance and updates (2026-09-11)

Procedure: [docs/reference_databases.md](docs/reference_databases.md). Goal: rebuild every reference database from the providers' general FASTA releases, under versioned names, so that the benchmark can be rerun when a new release comes out (decisions 14–17).

- [x] **S7.1** `[High / moderate]` `make_databases.R` in two idempotent steps: `download_reference_sources()` (`dbpq::download_unite_db(url, extract = TRUE)`, `dbpq::download_eukaryome_db()` then unzip and 7z; provenance in `data/data_raw/refseq/sources/manifest.csv`) and `derive_all_variants()` (`derive_dada2()` / `derive_sintax()` from the general FASTA, then the variants of `config.R::benchmark_dbs`, the fake reference and the `mini_*` subsets). `reference_sources` and `benchmark_dbs` in `config.R` drive `db_list`, `db_meta`, `seed_taxonomy_db`, `fake_ref_source` and `preference_db`; `pipelines/dada2.R`, chapters 00, 01 and 03 and the in silico notebook no longer hard-code database names. Download validated on 2026-09-11: both UNITE releases are byte-identical to the former `Unite.fasta` and `Unite_RefS.fasta`; the EUKARYOME v2.1 archives are nested (zip, then 7z, then FASTA). → [docs](docs/reference_databases.md)
- [x] **S7.2** `[High / easy]` Fungi-only variants anchored on the kingdom field (`derive_kingdom_only()`): the former `"Fungi"` pattern kept 965 non-Fungi records in `EUK_ITS_v2_Fungi`, 22 in `EUK_SSU_v2_Fungi` and 19 in `Unite_Fungi`. → [07 §B16](critique_fable/07_results_validity.md)
- [x] **S7.3** `[Medium / easy]` dbpq: `download_unite_db()` gains `url` and `extract` (tests, NEWS, man page; uncommitted on the dbpq `dev` branch). `format2dada2()` drops the UNITE kingdom like `format2sintax()`, so `derive_dada2()` applies the same `|k__` rewrite.
- [x] **S7.4** `[Critical / moderate]` Run `derive_all_variants()` on the downloaded sources (long: dbpq format conversion of the 1 877 003 EUKARYOME ITS v2.1 records per format, then cutadapt), and check the first header of each new file (docs §4). Done 2026-09-11 (10:40–10:54): the 20 dada2/sintax files and the 16 `mini_*` files (10 000 records each) start with the kingdom rank.
- [x] **S7.5** `[Low / easy]` Once S7.4 is validated, delete the legacy reference files listed in `data/README.md`. Done 2026-09-11: 40 files, 14.3 GB, including `refseq/Euk_ITS_v2_1.fasta`, an earlier download identical (md5) to `sources/EUK_ITS_v2.1.fasta`; `data/data_raw/refseq/` now holds 9.9 GB.
- [ ] **S7.6** `[Medium / easy]` Report upstream the dbpq defects listed in `proposals_for_dbpq.md` §0 (UNITE separator in `format2sintax()` / `format2dada2()`, kingdom-anchored `filter_db()`, `timeout = Inf` warning, nested EUKARYOME archives, EUKARYOME name qualifiers passed through by the format conversion — a dbpq matter, not a MiscMetabar one).
- [x] **S7.7** `[High / easy]` Negative-control reference and single-project runs (2026-09-11). `derive_all_variants()` had kept the May 2026 `fake_ref_asv_100.fasta` (idempotence): regenerated with `derive_fake_ref(force = TRUE)` from `Unite_s_all_20250219_wo_Fungi.fasta` (100 records, 73 phyla, no Fungi, 80 sequences shared with the old file). `pipelines/assign_taxo.R` now tracks it as the file target `fake_ref_file`, so a new fake reference invalidates `d_asv_for_assignation` and every assignment. `run_project()` moved from `make.R` to `R/run_project.R`: the docs told users to call it "from `make.R`", but sourcing `make.R` runs every production project. → [docs §5–6](docs/reference_databases.md)
- [x] **S7.8** `[Critical / easy]` Validated 2026-09-11: EUKARYOME files rebuilt, mini stores rerun, no "NAs introduits" warning and no parenthesis left in the 84 genus columns. EUKARYOME name qualifiers (`g__Lactarius(Fungi)`, `g__(Candida)`, `g__Mortierella.s.str`, `f__Xxx.nom.prov`; 11 413 ITS and 5 586 SSU v2.1 headers, already in v2.0): sintax bootstraps parsed as NA (genus never filtered by `min_bootstrap`), dada2 and lca genera that never match the mock (`Lactarius`, `Cryptococcus`, `Mortierella`, `Peziza`). Decision 2026-09-11: normalise at derivation. Code done: `general_headers_fixed()` keeps the name only before `derive_dada2()` / `derive_sintax()` (tested). Open: rebuild the EUKARYOME files with `force = TRUE`, invalidate the `EUK_` targets of the mini stores, rerun. → [07 §B17](critique_fable/07_results_validity.md)
- [x] **S7.9** `[Medium / moderate]` Database files are plain strings in `values_map`, so a database rebuilt in place (same name) does not invalidate its targets. Track them as file targets (one per `db_path`), like `d_asv_file` and `fake_ref_file`, so that `tar_make()` alone reruns what changed. Done 2026-09-11: `ref_file_target_for()` and a `ref_file` column in both builders of `R/values_map.R`; `ref_file_targets` (16 `format = "file"` targets, `ref_sintax__<db>` / `ref_dada2__<db>`) in `pipelines/assign_taxo.R` and `pipelines/cross_val.R`, whose commands now read `ref_fasta = ref_sintax__<db>`. Target names of the assignments are unchanged (tests pin them), but their commands changed, so every assignment and CV target is outdated once.

### Phase 8 — Compute budget (2026-09-11)

What the pipeline computes, where the time goes and the levers: [docs/experimental_design.md](docs/experimental_design.md) §3 (measured) and §8 (structure and levers, from the former `compute_budget.md`, deleted 2026-09-22). Today: ~5 h for `assign_taxo` (192 targets, single-threaded, dada2 on one worker) and, for `cross_val`, weeks with `cv_max_seq = NULL` or ~7-8 h with 10 000.

- [x] **S8.1** `[High / easy]` L1: compute lca once per database × input; its three `min_bootstrap` rows are identical (lca has no bootstrap: 24 `Genus_lca` columns, 7 distinct contents on the mini store). Done with S8.2.
- [x] **S8.2** `[High / moderate]` L2-L4: one dada2 (`outputBootstraps = TRUE`), one sintax and one blast computation per database × input, thresholds and votes applied afterwards in derive targets that keep the current `full_name`s; check equivalence on the mini store. Done 2026-09-11: `R/assign_compute.R` (`compute_assignment()`, `derive_assignment()`), `compute_name` column of `values_map`; `pipelines/assign_taxo.R` runs 64 computation targets (`compute_<method>__<db>`, `itsx_compute_…`) on the crew workers and derives the 192 rows in the main process; `benchmark_costs` joins the logs on `compute_name` (rows of one computation share its cost). blastn votes use the new `blast_table` argument of `MiscMetabar::assign_blastn()` (dev branch: code, test, NEWS, man). Equivalence with `add_new_taxonomy_pq()` checked on mini databases: lca, sintax (vsearch `--randseed 1`) and blastn (three votes) identical; dada2 thresholding identical to `assignTaxonomy(minBoot = )` on the same bootstraps (dada2 itself is not reproducible run to run). While doing it: B19 (S6.7).
- [x] **S8.3** `[High / easy]` L5: pass `multithread` / `nproc` (all methods currently run on one core) and size `n_workers × n_threads` to the 6 cores. Done 2026-09-11: `config.R::assign_threads_dada2` (3) and `assign_threads_fast` (1 per `fast_ctrl` worker, 3 workers).
- [x] **S8.4** `[Medium / easy]` L6: `nproc` in `run_cv()`; choose `cv_max_seq` and `cv_fold_tested` for the publication run (methods choice). Threads done 2026-09-14: `config.R::cv_threads` (2, with `n_workers = 3`) goes through `run_cv(nproc = )` to `cross_val()`, which passes it to sintax / lca, to blastn (`assign_blastn(nproc = )` → `blast_pq()`) and as `multithread` to dada2 (previously single-threaded). Folds decided 2026-09-14 (developer): every fold of a 10-fold CV (`cv_fold_tested = cv_fold_number`) on 5000 sequences per database (`cv_max_seq = 5000L`). Literature: 10-fold in Bokulich et al. 2018 (q2-feature-classifier, stratified by taxonomy, taxa with < 10 records amalgamated) and Allard et al. 2015; 2- and 5-fold in Lan et al. 2012, 5-fold in Kaehler et al. 2019; leave-one-out in Wang et al. 2007 (RDP) and Murali et al. 2018 (IDTAXA). Edgar 2018 (PeerJ 6:e4652) argues that leave-one-out and k-fold overestimate low-rank accuracy because test sequences stay close to the training set, and proposes cross-validation by identity. Unlike these studies, `cross_val()` draws the 5000 sequences and the folds at random, without stratification. Consequence measured on the full databases (10 random folds, seed 22): the share of test sequences whose genus is still in the training part is 96–100 % on the whole database but 66 % (`Unite_s_all_20250219`, `Unite_all_20250219`), 79–87 % (`_Fungi`, `EUK_ITS_v2.1`) and 88–94 % (`_Fungi_cut`) with 5000 sequences (76–96 % with 10 000). The standard variant therefore mixes known-genus queries with a database-dependent share (6–34 %) of novel-genus queries, which favours the small `_Fungi_cut` databases; state it in the Methods or stratify the subsample (not done, methods choice). The `*_mini` projects switch to 2 folds and 200 sequences automatically (`mini_db`, developer request of 2026-09-14).
- [x] **S8.5** `[Medium / easy]` L7-L8: decide whether the ITSx axis and the CV grid run on every database (design choice, decisions 19 and 7). Decided 2026-09-14 (developer): the ITSx input runs on the Fungi-filtered databases only (`config.R::itsx_db_list`, the `_Fungi` and `_Fungi_cut` variants, 5 of 8), so `values_map` has 156 rows (96 raw + 60 ITSx) and 52 computations; `build_values_map(itsx_dbs = )`, tests updated. The CV grid stays complete (4 methods × 8 databases × standard / leaked).
- [ ] **S8.6** `[Critical / easy]` First production night (2026-09-14/15): `dada2` OK (4 min; `d_asv` now 196 ASVs, all among the 200 of the 2024 store, 4 lost; the `s_d` warning is the stray `data/data_raw/rawseq/SRR30413326` folder), `assign_taxo` OK (1 h 34, 240 targets, 0 error, no empty assignment, check script clean), `cross_val` stopped after 2 h 11 (46 CV targets built, 18 not built: 12 dada2 targets, whose runs were cancelled, and the 6 `EUK_ITS_v2.1` ones): the crew worker of `cv__dada2__EUK_ITS_v2.1__leaked` crashed 6 times. Cause reproduced: `dada2::assignTaxonomy()` crashes the R process on long queries (15 222 bp passes; 20 040 bp gives "stack smashing detected", 32 759 bp a segfault), and in cross-validation the queries are reference sequences: `EUK_ITS_v2.1` holds 1309 sequences > 10 kb (max 32 759 bp), `EUK_ITS_v2.1_Fungi` 6, the UNITE and `_cut` databases none (max 3.2 kb). Not memory: no kernel OOM, the read step peaks at 2 GB. Options (methods choice): a `max_seq_length` filter in `cross_val()` next to `min_seq_length` (e.g. 10 000 bp, same queries for every method; reruns the 64 CV targets because the function changes), or skip the dada2 × `EUK_ITS_v2.1` CV cells. Also: `cross_val()` draws the `max_seq` subsample before `set.seed(seed)`, so the subsample follows the targets seed, not `seed`; the old May targets (`cv__*__Unite__*`, `EUK_ITS_v2`) remain in the store until a successful `tar_prune()`. Update 2026-09-15: trimming the CV queries to the amplicon (D1a, developer proposal) removes the long queries if records without the ITS2 site are dropped (the reverse complement of ITS2 is found in 8 of the 1309 `EUK_ITS_v2.1` records > 10 kb). Implemented 2026-09-15 (decision 24): queries without the ITS2 site are no longer queried, so the long records only remain as training references (dada2 read them as references without crashing in `assign_taxo`); `cross_val()` sets `seed` before drawing the subsample. To confirm on the next `cross_val` run. Second production pass (2026-09-15 afternoon, developer): `assign_taxo` OK (0 error, 0 warning, 0 outdated, 54 targets rebuilt: the `_cut` computations and `otu_clusters`, 195 ASVs in 143 OTUs, 29 with more than one ASV). `cross_val` stopped at 15:50 after 40 CV targets (0 error; at 15:58 no crew worker was left and 3 targets were still marked dispatched; no dada2 target finished, so the long-query crash is still to confirm) because of the sintax row-order bug (D1a, B23). Genus results of the valid partial targets, to re-check on the full run: standard blastn 0.04–0.12 good with 87–95 % NA, standard lca 0.41–0.61 good with 38–57 % wrong; leaked blastn 0.98, lca 0.99–1.00. Mock: dada2 on the full `EUK_ITS_v2.1` assigns a genus to 0 % of the mock ASVs (Kingdom 10.8 %, mostly Metazoa / Viridiplantae) while it reaches 95.4 % on `EUK_ITS_v2.1_Fungi`; see the database-building item on non-Fungi references. Rerun the same evening (2026-09-15, 16:03–16:50, developer): 56 CV targets, 0 error, 0 warning, 47 min of wall time for 2.3 h of compute (mean per target: dada2 5.0 min, blastn 2.4, lca 2.2, sintax 0.2; maxima 8.8, 3.1, 9.3, 0.4). **The dada2 long-query crash is gone**: the six `EUK_ITS_v2.1` dada2 targets completed, since the records without ITS2 site (99.4 % of the records > 10 kb) are no longer queried. Genus, leaked variant: sintax 0.999–1.000 (B23 fixed), lca 0.993–1.000, dada2 0.958–0.998, blastn 0.969–0.986, so every method finds its own record. Genus, standard variant: lca 0.38–0.62, sintax 0.26–0.52, dada2 0.20–0.43, blastn 0.04–0.12; the Fungi filter helps every method on EUKARYOME (dada2 0.20 → 0.42, sintax 0.26 → 0.51). Two things to look at before the figures: blastn leaves 87–95 % NA in the standard variant (S1.3), and lca gives a wrong genus to 37–60 % of the queries there (it has no identity threshold), against 11–24 % for sintax and dada2 and ≤ 3 % for blastn. **Both are explained by the reference size (B24, found 2026-09-16)**: `cross_val()` keeps only the drawn pool as reference (5 413 records of the 156 820 of `Unite_all_20250219`, 3.45 %; 6 284 of the 1 877 003 of `EUK_ITS_v2.1`, 0.33 %; 5 295 of `EUK_ITS_v2.1_Fungi`, 0.49 % — the pool grows until 5 000 of its records carry the ITS2 site, so its size follows that site rate and never the database size), which is the documented behaviour of `max_seq` but makes the CV measure a small reference rather than the benchmarked database. blastn abstains when nothing passes its filters, lca and sintax return the nearest available taxon and are wrong instead. The CV database axis therefore compares taxonomic composition at equal size, not the databases as used in the mock. Open: chapters 01 and 06 to re-render.

### Answers to the open questions (2026-09-10)

- `figures/` is ignored entirely (`figures/*` + `!figures/.gitkeep`); figures live in the manuscript folder. Applied.
- `store_*/meta/meta` stay tracked; `tar_prune()` in `make.R` keeps the diff small.
- The reference MiscMetabar (and comparpq, dbpq, greenAlgoR, tidypq) is the dev checkout under `pqverse_pkg/`; S2.2 adds a `session_info` target to pin the commit.
- `docs/references/41576_2023_679_MOESM2_ESM.ods` is kept as is, source still to be filled in `docs/references/README.md`.
- `SRR30413326` is a planned second mock community (D1d below). `mock_hleap2021/` (Hleap 2021's COI animal mocks) was deleted on 2026-09-17 (developer): out of scope for fungal ITS.

---

## 2. Open scientific work

### Little side questions to explore

#### Software parameters

What is the effect of set --maxaccepts 16 in sintax ?
What is the effect of set --dbmask none in sintax ?

#### Databases building

What is the effect of taxing the Unite database with singletons as RefSeq only (Unite_RefS.fasta) ? Now benchmarked as `Unite_all_20250219` against `Unite_s_all_20250219` (decision 17); the former `Unite_RefS.fasta` was in fact the release without singletons.

- [x] `[Critical /easy]` **Decided 2026-09-17 as the `fungi + rep` level of the grid, implemented in item 0.2 (ticked 2026-09-22 at the ROADMAP split).** What is the effect of adding only some sequences to a FUNGI filtered database ? We must add a 8th and 9th databases using EUK_fungi and Unite_fungi with FUNGI + some outgroup sequences from non-fungal sequences samples from each Phylum to have representative sequences for all broad taxonomic cases. We must also add some non-fungal sequences in mock community ?  

- [x] `[High / easy]` **Closed as a Q2 result, not a defect** (cause measured 2026-09-16, decision the same day; ticked 2026-09-22 at the ROADMAP split — what remains is to promote it to the Results, `ROADMAP.md` C22-P5). dada2 fails on the full `EUK_ITS_v2.1` (found 2026-09-15 on the production `assign_taxo`): a genus for 0 % of the mock ASVs at 0.5 (Kingdom 10.8 %, mostly Metazoa / Viridiplantae), against 95.4 % on `EUK_ITS_v2.1_Fungi`; sintax (97.4 %) and blastn (99.5 %) are not affected, lca drops to 66.2 %. Test on the 195 mock ASVs (`assignTaxonomy()`, `minBoot = 50`, `mini_EUK_ITS_v2.1_Fungi` = 10 000 Fungi records): 96.4 % genus alone; 50.8 % once the 7871 `EUK_ITS_v2.1` records > 5 kb are added (158 ASVs then get the kingdom `_mitochondrion`); 9.7 % once 7871 random non-Fungi records ≤ 5 kb are added instead (Kingdom 19 %). The lengths of the added short records were not controlled, so the share of length and of taxonomic breadth in this drop is not separated. The EUKARYOME source files organelle genomes under the kingdom `_mitochondrion` (`k___mitochondrion;…;g__Rhizophagus;s__irregularis`), and 1248 of its 1309 records > 10 kb are such genomes, including fungal ones that the `_Fungi` filter (kingdom exactly Fungi) removes. **Cause measured 2026-09-16, and it is neither the format nor the organelles** (developer's question): the dada2 files of `EUK_ITS_v2.1`, `_Fungi`, `_Fungi_cut` and UNITE share the same header format (7 fields, trailing semicolon, 100 % of the records). In the raw computation stored in `compute_dada2__EUK_ITS_v2.1`, before any threshold or cleaning, dada2 assigns **every** ASV: 0 % NA, 100 % with a Kingdom value and 84.7 % with a real (non-`unclassified`) Genus. What collapses is the bootstrap: median 31 at Kingdom and 17 at Genus, against 100 and 68 on `EUK_ITS_v2.1_Fungi`. The benchmark thresholds (0.4, 0.5, 0.6) therefore wipe the whole assignment, which is the 0 % seen in `d_all_taxo`. The driver is the taxonomic breadth of the full release (336 distinct kingdom values, 26 637 genera, 47.4 % of the records `unclassified` at Genus, against 1, 6 801 and 40.6 % in `_Fungi`): with many competing non-fungal taxa, no candidate wins enough bootstrap replicates. Consistent with the mini test, where adding non-Fungi records ≤ 5 kb (arm C, genus 9.7 %) hurt more than adding the records > 5 kb (arm B, 50.8 %). A Q2 result for the article: dada2 is the only method whose bootstrap makes it abstain on a broad reference, while sintax (97.4 %), blastn (99.5 %) and lca (66.2 %) still assign. Decision 2026-09-16 (developer): keep `EUK_ITS_v2.1` as it is, organelles included (2 010 `_mitochondrion` records, 0.11 %, and 118 `_plastid`).

### D1a — Cross-validation (remaining)

- [ ] CV figures parallel to Q1 & Q2 (F1 per rank, per method × DB). Code in `analysis/06_cross_validation.qmd`, but `store_cross_val` itself is invalid (S6.1, S6.4): rerun `cross_val` with S6.5 first.

Exemple of a CV figure:

```{r}
cv_res <- tar_read("cv_results", store="store_cross_val")
cv_res |> dplyr::filter(metric=="good_classifications") |> arrange(desc(mean)) |> ggplot(aes(x=mean, y=factor(method), fill=db)) + geom_violin() + facet_grid(min_bootstrap~tax_level)
```

- [x] Known limitation carried from `R/cross_val.R`: the `dada2` branch calls `assignTaxonomy` with `minBoot = 0` and never applies `min_bootstrap` post-hoc; the `dada2_2steps` branch `stop()`s. Decide: fix, or document as "dada2 CV = no bootstrap filter" in Methods. Fixed 2026-09-11 (a bug, not a Methods choice). With one `min_bootstrap`, dada2 now gets `minBoot = 100 * min_bootstrap`, and sintax gets `min_bootstrap` too: `assign_sintax()` had silently applied its own default of 0.5. With several values, the dada2 bootstraps are rescaled to 0–1 and each row carries its own threshold (every row was labelled with the last one). Check on `mini_Unite_s_all_20250219_Fungi` (600 records, one fold): genus NA proportion 0.03 at 0.1 and 0.70 at 0.9 for dada2, 0.17 and 0.75 for sintax. `store_cross_val_mini` must be rerun (the change to `cross_val()` invalidates its targets). `dada2_2steps` still stops.
- [x] `[High / moderate]` Developer proposal (2026-09-15): pass the CV query sequences through cutadapt, so that they look like the ASVs of an Illumina run. Facts: `d_asv` holds no primer site (`cutadapt_remove_primers()` removes both primers and their reverse complements); reference records rarely start before ITS1F (0.1 % of `Unite_s_all_20250219_Fungi`, 2 % of `EUK_ITS_v2.1_Fungi`) but ≈ 94 % hold the reverse complement of ITS2, so trimmed queries would be ≈ 210 bp (ITS1 + start of 5.8S) without the 30–45 bp of 18S that start the real ASVs (B18); the records holding both sites give a 185 / 212 / 239 bp amplicon (5 / 50 / 95 %) on UNITE Fungi. To decide: trim the test queries only (the training part stays full length, as the references are for the mock) or both parts; rule for records without the ITS2 site (≈ 6 % of the Fungi databases, 22 % of `EUK_ITS_v2.1`, 99.4 % of its 1309 records > 10 kb): dropping them also removes the dada2 long-query crash (S8.6) but changes the taxonomic composition of the subsample (S8.4). Uses the same trimming code as the S6.9 fix. **Decided and implemented 2026-09-15** (decision 24): test queries only, records without the ITS2 site never queried but kept in the training part, exactly `cv_max_seq` queries by drawing `cv_oversample = 2` records per wanted query. `R/cv_queries.R` (`trim_cv_queries()`, `cv_select_queries()`, `tests/test_cv_queries.R`), new `cross_val()` arguments `primer_fw`, `primer_rev`, `primer_min_overlap`, `oversample`, `cutadapt_prelude`, passed by `pipelines/cross_val.R` (which now loads dbpq). `cross_val()` also sets `seed` before drawing the subsample (S8.6). On 3000 random records, 93.8 % (`EUK_ITS_v2.1_Fungi`), 95.4 % (`Unite_s_all_20250219_Fungi`) and 80.9 % (`EUK_ITS_v2.1`) hold the site; query median 207, 214 and 227 bp. Smoke test on `mini_EUK_ITS_v2.1` (sintax, 2 folds, `max_seq = 200`): 400 drawn, 373 with the site, 217 records kept, 200 queries. Identical amplicons checked on 5000 trimmed queries (the per-fold `!duplicated()` of `cross_val()` keeps the first name, hence its taxonomy): 1 to 7 groups of identical amplicons per database (`Unite_s_all_20250219_Fungi` 2, `Unite_s_all_20250219` 1, `EUK_ITS_v2.1_Fungi` 7, `EUK_ITS_v2.1` 4), one group with two genera (`EUK_ITS_v2.1`), 0 or 1 query dropped by the per-fold deduplication: no change needed. `_Fungi_cut` rule (developer, 2026-09-15 afternoon, after `cross_val_mini` stopped on 0 query): the records of a `_Fungi_cut` database no longer hold the primer sites, so its queries are drawn and trimmed from its `_Fungi` source (`cross_val(query_fasta = )`, columns `query_db`, `query_db_path`, `query_ref_file` of `build_cv_values_map()`, query files added to the CV file targets) and paired by name with the `_cut` records, which form the training part; a query whose amplicon differs from its paired record is dropped. Smoke test on `mini_EUK_ITS_v2.1_Fungi_cut`: sintax 400 drawn, 373 paired queries with the site, 200 queries; dada2 44 drawable records, 41 queries (fixed by the next item). `cross_val_mini` run by the developer (2026-09-15, 13:50): 56 CV targets, 0 error, 0 warning, 7 databases. Genus good classifications, mean over databases: leaked variant 1.00 for dada2, lca and blastn and 0.80 for sintax, so the trimmed queries are paired with their truth; standard variant 0.12 (dada2), 0.30 (sintax), 0.47 (lca), 0 (blastn), with 69 %, 51 %, 2 % and 100 % NA (mini subsample of about 200 records, few congeners; blastn finds no hit, S1.3). Open: production `cross_val`, then check blastn there.
- [x] **[High / easy]** dada2 cross-validation draws one record per taxonomy string (found 2026-09-15). `cross_val()` removes duplicated names before drawing, and the dada2-format headers are the taxonomy only (no identifier), so dada2 can draw 22.8 % of `Unite_s_all_20250219_Fungi` (38 310 of 168 030 records), 5.7 % of `EUK_ITS_v2.1_Fungi` (61 710 of 1 076 870) and 11 % of `EUK_ITS_v2.1` (206 399 of 1 877 003), while sintax, lca and blastn draw from every record (sintax-format headers carry an identifier); on the mini files, 44 of 10 000 records. In the standard variant every training record then has a distinct taxonomy string, so the exact taxonomy of a query is never in its training part, and the dada2 CV results are not comparable with the other methods (every CV store so far). **Fixed 2026-09-15** (developer: before the CV run). Both formats hold the same records in the same order (checked on the 7 databases and their `mini_*` files: same counts and lengths, 2000 random sequences and taxonomy strings identical, unique sintax identifiers), so `cross_val(id_fasta = , query_id_fasta = )` names the dada2-format records with the sintax-format identifiers (`record_ids()` in `R/cv_queries.R`, which checks the lengths record by record) and keeps the dada2 headers for the truth table (`create_fake_pq_from_refseq(headers = )`) and the training fasta; columns `id_db_path`, `id_ref_file`, `query_id_db_path`, `query_id_ref_file` of `build_cv_values_map()` (the sintax file of the same database for every method); tests (202 expectations in all). Smoke test on the mini files (1 fold of 10, `max_seq = 200`): dada2 on `Unite_s_all_20250219_Fungi` 366 usable queries of 400 drawn (44 drawable records before the fix), dada2 and sintax on `EUK_ITS_v2.1_Fungi_cut` 373 of 400, 200 queries each. Every CV target reruns. → [07 §B22](critique_fable/07_results_validity.md)
- [x] **[Critical / easy]** sintax cross-validation compares shuffled rows (found 2026-09-15 on the production `cross_val` run: sintax leaked genus 0.49–0.54 with 0 % NA, lca and blastn 0.98–1.00). `cross_val()` compares the assignments to the truth by position, and vsearch `--sintax` with several threads writes its results in completion order: on 2000 leaked queries of `mini_Unite_all_20250219`, 1.000 good genus by position with `nproc = 1`, 0.237 with `nproc = 4` (1.000 once joined on `taxa_names`). Every sintax CV target since the CV threads (S8.4) is invalid, including `cross_val_mini` of 2026-09-15; the mock assignments join on `taxa_names` and are not affected. Fixed the same day (developer choice: in the project, rerun everything): `cv_align_rows()` in `R/cv_queries.R` (test in `tests/test_cv_queries.R`) orders the sintax values, bootstraps and lca rows as `taxa_names(fake_pq)`, and `cross_val()` stops when a method returns another number of rows than queries. Smoke test through `cross_val()`: sintax leaked genus 1.00 with 1 and 4 threads, lca 0.998. The production run stopped after 40 CV targets; the 56 CV targets are outdated. No sintax record identifier holds whitespace (7 databases and their `mini_*` files), so vsearch label truncation cannot trigger the new `cv_align_rows()` stop. `cross_val_mini` rerun by the developer (2026-09-15, 16:00): 0 error, 0 warning; leaked genus good classifications 1.00 for the four methods on the seven databases (`_Fungi_cut` included); standard: sintax 0.15–0.70, lca 0.17–0.85, dada2 0.08–0.20, blastn 0–0.03 (mini subsample). Confirmed on the production run of 2026-09-15 (16:03–16:50, 56 targets, 0 error): leaked genus sintax 0.999–1.000 on the seven databases, against 0.49–0.54 before the fix. → [07 §B23](critique_fable/07_results_validity.md)
- [ ] **[Critical / moderate]** Choose the cross-validation reference design (open 2026-09-16, developer). Today `cross_val()` replaces the database by the drawn pool (B24, `docs/diagnostics_cv_and_databases.md` § 1): 5 413 records of 156 820 on `Unite_all_20250219`, 6 284 of 1 877 003 on `EUK_ITS_v2.1`, and 30 % of the UNITE queries lose every congener, which caps the standard variant whatever the method. Literature check (§ 8 of the same document): no cited benchmark uses a random subsample. Bokulich et al. 2018 keep the whole database minus the test fold, stratified by taxonomy (and a separate "novel taxon" run excluding the query's taxon at rank *L*); Murali et al. 2018 (IDTAXA) use leave-one-out; Edgar 2018 (TAXXI) splits by identity and reports per identity bin; Hleap et al. 2021 subsample **by taxonomy** (one representative per species in the relevant clade) and only for alignment-based methods. Options and costs (§ 7): whole database minus the fold, first estimated at ≈ 63 h of compute and 20–25 h of wall time (**estimate withdrawn 2026-09-16**: it assumed ten folds and mock timings on 392 ASVs; the design now uses three folds and dada2's duration on a full EUKARYOME reference is unmeasured), against 47 min today; taxon-aware pool keeps the current cost but its pool size is unmeasured; keeping the pool means writing in the Methods that D1a measures a fixed-size reference. **Decided 2026-09-16 (developer): Bokulich design, removing only the tested queries** (not 10 % of the database per fold), and **no stratified draw** — the CV compares algorithms × databases × metrics against each other, not as an absolute confidence value; the closeness to a flattering leave-one-out is accepted and documented. Consequence to document instead of stratifying (§ 2 of the diagnostics document): a query whose genus has a single record in the database stays unclassifiable at genus, which is 4.2–4.9 % of the UNITE records against 0.1–0.8 % of the EUKARYOME ones, so UNITE shows structurally more genus NA; conversely EUKARYOME carries fewer usable genus labels (52.6 % of the full release against 73.0 % for `Unite_all_20250219`). **Not reachable by parameters alone**: `max_seq` drove both the query count and the reference reduction (`R/cross_val.R:197`). **Implemented 2026-09-16**: `cv_reference_records()` in `R/cv_queries.R` (tested), the `reduce_reference` argument of `cross_val()` (default TRUE, so older callers are unchanged), `config.R::cv_reduce_reference = FALSE`, `cv_fold_number = 3L` (three folds, since removing 5 000 queries from 156 820 no longer justifies ten index rebuilds), and the `run_cv()` / target passthrough in `pipelines/cross_val.R`. The queries are drawn exactly as before (same pool, same seed, same trimming): only the reference changes. Tests pass (10 files) and `cross_val_mini` rebuilds its 56 targets without error; genus means over the seven mini databases, standard variant: lca 0.754, sintax 0.726, dada2 0.655, blastn 0.310 (against 0.17–0.85, 0.15–0.70, 0.08–0.20 and 0–0.03 with the reduced reference), leaked 0.965–1.000. The 56 production targets are outdated. **Memory measured 2026-09-16 and it blocks the production run as configured**: one dada2 CV target on the full `EUK_ITS_v2.1` reference (1 877 003 records, 500 queries, one fold) peaked at **45.0 GB** and had to be killed at 7 min 58 before finishing its fold, with the machine at 60 of 62 GB used and 22.9 GB of swap consumed. `pipelines/cross_val.R` runs `n_workers = 3` targets on a single crew controller, so three such targets would be dispatched together: impossible. Options (developer choice): give dada2 its own one-worker controller as `pipelines/assign_taxo.R` does (`dada2_ctrl` / `fast_ctrl`), and/or drop the full `EUK_ITS_v2.1` from the CV grid (the mock already covers it), and/or keep `reduce_reference = TRUE` for that database only (inconsistent and hard to defend in the Methods). Second point measured the same day: dada2 × `Unite_s_all_20250219` (266 589 records) peaks at **23.5 GB** and completes a fold in 4 min 32 (genus standard: 0.335 good, 0.138 wrong, 52.7 % NA). So memory is strongly sublinear in record count (7× the records for < 2× the memory) but never small: **every dada2 CV target with a full reference costs 23–45 GB**, three simultaneous workers are impossible on 62 GB and two are already risky. Recommendation: split `pipelines/cross_val.R` into `dada2_ctrl` (1 worker) and `fast_ctrl` (`n_workers`) as `pipelines/assign_taxo.R` does, so at most one dada2 target is resident while sintax / lca / blastn (which stream their reference through vsearch or BLAST) keep the other cores busy. Even then dada2 × `EUK_ITS_v2.1` alone needs ≥ 45 GB and did not finish one fold in 8 minutes: decide whether to keep that cell, since the mock already covers the full release. **Implemented 2026-09-16** (developer decisions: keep the dada2 × `EUK_ITS_v2.1` cell, it complements the mock; split the controllers): `crew_controller_group` in `pipelines/cross_val.R` and a `controller` column in `build_cv_values_map()` route 14 dada2 targets to `dada2_ctrl` and 42 to `fast_ctrl`; tests pass and the mini manifest builds. **Third measurement the same day**: sintax × `EUK_ITS_v2.1` peaks at only **5.9 GB** in 2 min 26 (vsearch streams its reference, dada2 loads it), and reaches 0.790 good genus, 0.054 wrong, 15.6 % NA against the full database — against 0.26–0.52 with the reduced reference, which is the clearest evidence so far that the B24 fix changes the nature of the results. **Budget still open**: the worst case is one dada2 target on `EUK_ITS_v2.1` (≥ 45 GB) beside three fast targets, i.e. 45 + 3 × 5.9 ≈ 63 GB on a 62 GB machine. Two fast workers for this project would give 57 GB. blastn measured right after: **3.5 GB**, 1 min 48, genus 0.737 good, 0.072 wrong, **19.2 % NA** against the full database. lca measured last: **6.2 GB**, 7 min 05, genus 0.754 good, **0.150 wrong**, 9.6 % NA — it abstains least and errs most, where blastn does the opposite (19.2 % NA, 7.2 % wrong) and sintax sits between the two. Budget now fully measured, no projection left: three fast workers give 45 + 6.2 + 5.9 + 3.5 = **60.6 GB on a 62 GB machine**, two give **57.1 GB**. The dada2 figure is a floor (its run was killed before finishing), so **two fast workers** is the safe configuration — through a constant dedicated to this project, since `n_workers` is shared with `pipelines/assign_taxo.R`. **Decided and implemented 2026-09-16 (developer): two fast workers**, `config.R::cv_n_workers_fast = 2`, read by `pipelines/cross_val.R` only; `assign_taxo` keeps `n_workers = 3`. Two caveats on the arithmetic, to watch on the first dada2 targets rather than trust: `dada2_ctrl` holds one worker for 14 dada2 targets, so a dada2 target is resident almost continuously (the worst case is the normal case), and `seconds_idle = 60` does not guarantee a fresh worker per target — R returns freed memory to the system lazily, so a worker may still hold an EUKARYOME reference when the next target starts. → [07 §B24](critique_fable/07_results_validity.md)
- [ ] From the TODO block in `R/cross_val.R`: add fake sequences to CV folds to obtain a true-negative rate (trade-off analysis); consider `nperm` permutations on top of k-fold (probably too slow). `[Low / moderate]`

### Q4 — ITS extraction of the ASVs with ITSx (proposed 2026-09-11)

Origin: B18 ([07](critique_fable/07_results_validity.md)). The ITS1F–ITS2 ASVs keep 30–45 bp of the 18S end, absent from ITS references; this penalises blastn (query cover) and may affect the k-mer methods and the uncut vs `_cut` database comparison. Extracting ITS1 with ITSx before assignment adds an ASV-preprocessing axis to the benchmark.

- [x] `[Medium / easy]` Install ITSx (bioconda; not on `PATH` nor in `cutadaptenv` on 2026-09-11) and record the version and the command prelude in `config.R`, like `cutadapt_conda_prelude`. Done 2026-09-11: ITSx 1.1.3 in the `itsxenv` conda env; `config.R::itsx_conda_prelude`, `itsx_region` (`ITS1`), `itsx_organism_groups` (`all`: ITSx only trims the flanks, it is not used to keep Fungi only).
- [x] `[Medium / moderate]` Add an ITSx step producing `d_asv_itsx` (ITS1 of each ASV) and report the ASVs ITSx does not detect. Done 2026-09-11 in `pipelines/assign_taxo.R` rather than `pipelines/dada2.R`, so the step runs from the stored `d_asv` without rerunning DADA2: `itsx_asv` (`run_itsx()`, `R/itsx.R`, keeps the ITSx positions table) → `d_asv_itsx` (`itsx_replace_refseq()`: undetected ASVs keep their full sequence and are reported in a message). On the 200 mock ASVs: 200 detected as fungal ITS, SSU at positions 1–45, ITS1 kept. Tests: `tests/test_itsx.R`. No pqverse wrapper yet: candidate for MiscMetabar, next to `cutadapt_remove_primers()`.
- [x] `[Medium / moderate]` Decide where ITSx sits relative to the negative controls. Decision 19: ITSx on `d_asv`, then the same `add_shuffle_seq_pq()` and `add_external_seq_pq()`; both shuffles run under `withr::with_seed(targets_seed, …)`, so the same ASVs are shuffled and both inputs carry the same `fake_1..n` and `external_*` taxa.
- [x] `[Medium / moderate]` Benchmark axis: assignments on `d_asv` vs `d_asv_itsx`. Decision 19: full grid, same store. `build_values_map(preprocess = c("none", "itsx"))`: 12 method/parameter rows × 8 databases × 2 inputs = 192 assignment targets; the ITSx ones are prefixed `itsx_` (`itsx_sintax__EUK_ITS_v2.1___0.5`), the names of the others do not change, and `input_pq` selects `d_asv_itsx_for_assignation`. `01_load_and_clean.qmd` parses `itsx_<method>` as a method and keeps these columns out of the Q3 consensus. Revised 2026-09-14 (S8.5): ITSx rows on the five Fungi-filtered databases only, 156 rows.
- [ ] `[Medium / easy]` Figures: F1 / MCC per rank with and without ITSx, per method × database; check whether the blastn gap closes and whether `min_cover` can return to 95 on ITSx-extracted ASVs.

### Q5 — Post-clustering of the ASVs into OTUs (proposed 2026-09-15)

Developer proposal: a `d_otu` object built with `postcluster_pq(d_asv, method = "vsearch")`, to test whether post-clustering improves taxonomic assignment. Facts measured on 2026-09-15 (`store_dada2`, `store_assign_taxo`):

- `pipelines/dada2.R` already builds it as `d_vs` (`asv2otu(method = "vsearch")`, 97 %, `tax_adjust = 0`): 143 OTUs from the 196 ASVs, the same taxa as a fresh `postcluster_pq(d_asv, method = "vsearch")`. 30 clusters hold more than one ASV (83 ASVs, 8 at most); the `otu_clusters` target of `pipelines/assign_taxo.R` clusters `d_asv_common` instead (195 ASVs, one lost to the ITSx deduplication of decision 20) and gives 143 clusters of which 29 hold more than one ASV: the two clusterings are close but not the same object; no cluster mixes seed genera, 3 mix two seed species (in *Verticillium*, *Fusarium*, *Zymoseptoria*, genera with 2, 15 and 4 species in the mock).
- `merge_taxa_vec(tax_adjust = 0)` keeps the most abundant ASV of each cluster: every OTU carries the name and the sequence of one ASV of `d_asv` (143 of 143). Assigning `d_otu` would reassign 143 sequences already assigned in `store_assign_taxo` (same result for lca and blastn, another bootstrap draw for dada2 and sintax). The OTU results can therefore be derived from `d_all_taxo` by keeping these 143 taxa and the same controls, without new computation.
- `tc_metrics_mock_vec()` counts taxa, not reads (TP and FP over the assigned units, FN over the rows of the truth table): with OTUs, TP and FP are counted over 143 units instead of 196 and FN can only grow. The comparison measures the unit count and the loss of the non-representative ASVs, not a better assignment of each sequence.
- An assignment can only change if the OTU taxonomy is built from its members (consensus or LCA of the member ASVs) or if the representative sequence changes. `combine_taxo_assignments()` needs the same taxa in every input (decision 20), so real `d_otu` assignments would need their own controls, `tar_combine()` and `method_db` names.

- [x] `[Medium / easy]` Decide (developer): derive the OTU results from `d_all_taxo` (no computation), assign `d_otu` in its own branch (+32 computations for 4 methods × 8 databases, +20 with ITSx on the Fungi databases; `assign_taxo` took 1 h 34 for 52), or define the OTU taxonomy from its member ASVs. Decided 2026-09-15 (developer, decision 25): taxonomy from the member ASVs, `unanimity`. Implemented: `R/otu_taxonomy.R` (`otu_membership()`, `resolve_otu_taxonomy()`, `tests/test_otu_taxonomy.R`), target `otu_clusters` in `pipelines/assign_taxo.R`; chapter 01 builds `d_otu_taxo` after the `Gen_sp` columns and `res_comp_tax_otu` (single-method columns, `unit = "OTU"`), both cached. Checked on the production store of 2026-09-15 (uncleaned `d_all_taxo` of the former 8-database grid): 392 taxa → 340 (143 OTUs, 29 with more than one ASV; the 197 controls unchanged), same 1138 columns, reads kept, 13 s; over the 29 multi-ASV OTUs × 96 raw Genus columns, unanimity set 110 archetype genera to NA and replaced none. Chapter 01 itself is untested until a store holds `otu_clusters`.
First production numbers (2026-09-16, `analysis/_cache`): at Genus, mean F1 0.782 over the 132 single-method ASV columns against 0.743 for the same columns on OTUs, and **no column where the OTUs do better** (0 of 132). Read with the caveat above: `tc_metrics_mock_vec()` counts taxa, so the 143 OTUs lose the TP carried by the non-representative ASVs while the truth table keeps its rows.

- [ ] `[Medium / easy]` Q5 figures: F1 / MCC per rank for ASVs and OTUs, per method × database (and per input); consensus strategies on OTUs only if needed.

### D1d — Second mock community, SRR30413326 (decision 12)

Truth table already on disk: `data/data_raw/metadata/taxo_mock_SRR30413326.csv` (16 fungal taxa: Russula ×3, Lactifluus ×3, Sydowia, Sporobolomyces, Phallus, …). `data/data_raw/rawseq/SRR30413326/` exists but is empty.

- [ ] Fetch the SRR30413326 fastqs into `data/data_raw/rawseq/SRR30413326/` (`fasterq-dump SRR30413326`), and record the study / primers / read layout in `data/README.md` (S3.2).
- [ ] Confirm the primer pair; if it differs from ITS-1F/ITS2, parameterise `script_dada2.R` (primers and sample-data file come from `config.R`; a `dataset` env var or a `_targets.yaml` project per dataset, see S3.1) rather than copying the script.
- [ ] Stand up `store_dada2_srr30413326` and `store_assign_taxo_srr30413326`, same `values_map` as the main run.
- [ ] Run `tc_metrics_mock()` against the second truth table; add the dataset as a facet/colour to the Q1 and Q2 figures (agreement between the two mocks is itself a result, as for CV).
- [x] Decide what to do with `data/data_raw/mock_hleap2021/Mocks/`: **deleted 2026-09-17** (developer). COI mocks of fish, insects and zooplankton, outside the fungal scope (decision 12 of `docs/objectives_design.md`).

### 🔄 Phase 4 — In silico simulations (manual actions)

| #   | Action                                                                                        | Command / location                                                                                                                                                     | Blocker for           |
| --- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| 6   | Curate input taxon list from UNITE Fungi (50–200 species, one ref seq each, spanning 7 phyla) | Manual selection from `data/data_raw/refseq/`                                                                                                                          | D1b-A InSilicoSeq run |
| 7   | Run InSilicoSeq to generate simulated fastqs (3 replicate seeds)                              | `iss generate --genomes mini_Unite.fasta --sequence_type amplicon --n_reads 10000 --abundance zero_inflated_lognormal --model MiSeq` (see `Archives/some_bash_script`) | D1b-A pipeline        |
| 8   | Run DADA2 + assignment on InSilicoSeq fastqs                                                  | add `dada2_insilico` / `assign_taxo_insilico` projects to `_targets.yaml` (same scripts, own stores), then `TAR_PROJECT=dada2_insilico` and `TAR_PROJECT=assign_taxo_insilico` `tar_make()`          | D1b-A analysis        |

### D1b — In silico simulations (two sub-branches per decision 3)

**D1b-A — InSilicoSeq path (fastq from fasta).**
- [ ] Pick a curated input taxon list from UNITE Fungi (e.g. 50–200 species, one ref sequence each, spanning all 7 phyla).
- [ ] Run InSilicoSeq via the existing Docker recipe (already drafted in `Archives/some_bash_script` and in `Taxonomic assignation.md`): `iss generate --genomes mini_Unite.fasta --sequence_type amplicon --n_reads 10000 --abundance zero_inflated_lognormal --model MiSeq`. Reproducibly via 3 replicate seeds. Environment checked 2026-09-16: `iss` is on no PATH and in none of the conda envs (base, cutadaptenv, figembed, itsxenv, ocr), while **Docker 29.5.3 is available**, so the archived biocontainer recipe (`quay.io/biocontainers/insilicoseq:2.0.0--pyh7cba7a3_0`) is the path of least resistance and needs no install. Step 2 of `analysis/in_silico_simulation.qmd` currently assumes `iss` inside `cutadaptenv` and must be rewritten as the Docker call (mount the project root, write to `data/data_raw/insilico/`).
- [ ] Feed the simulated fastqs through `script_dada2.R` → `d_asv` → existing assignment loop. The "truth" table is the input taxon list.

**D1b-A fastqs generated 2026-09-16 (developer)** through the Docker biocontainer: `data/data_raw/insilico/iss_seed{1,2,3}_R{1,2}.fastq`, 4 961 / 4 994 / 4 963 read pairs per seed (InSilicoSeq counts `--n_reads` as total reads, so half as many pairs), plus one `*_abundance.txt` per seed; 23 MB in total, with empty `*.iss.tmp.*.vcf` leftovers. `data/data_raw/insilico/` added to `.gitignore`, which covered every other generated data folder but not this one. Open: the `dada2_insilico` / `assign_taxo_insilico` projects in `_targets.yaml` (step 3 of the notebook), which need the per-dataset raw-read switch of D1d.

**D1b-B — miaSim path (community matrices). Dropped 2026-09-17 (objectives_design decision 12: no community ecology), removed from the code 2026-09-22 (item 0.9).** Its six items and the measurements they carried — the stratified phylum draw of the simulated community, the API repairs of the notebook, the first successful mini render — are in the git history of `ROADMAP.md` and of `analysis/in_silico_simulation.qmd`. One finding is worth keeping because it applies to D1b-A too: **drawing species uniformly from UNITE Fungi covers few phyla** (50 species gave 3 phyla, dominated by Ascomycota and Basidiomycota), while the 100-sequence D1b-A input fasta drawn the same way spans 9 phyla and 84 genera. If D1b-A is ever redrawn with fewer sequences, either stratify by phylum with a cap or say in the Methods that the community follows the reference's phylum frequencies rather than covering the kingdom.
- [x] **D1b shared** — done 2026-09-22: `analysis/in_silico_simulation.qmd` holds the single InSilicoSeq branch, its input fasta drawn from `config.R::insilico_source`, and a step 4 that parses the per-record truth out of the UNITE headers. It is the truth the per-unit scoring of `comparpq::tc_metrics_unit()` takes, matched to the ASVs by the rule of `R/mock_truth.R::sanger_matches()` — the same machinery as the mocks, not `tc_metrics_mock()`.
- [x] **[High / easy]** `analysis/in_silico_simulation.qmd` (D1b-B) was written against an obsolete API and could not run; repaired 2026-09-16 (`tc_metrics_mock()` signature, the `metrics` / `values` columns, `clean_pq = FALSE` for B19, the `BENCHMARK_ANALYSIS_MINI` switch, the two `add_shuffle_seq_pq()` preconditions) and then **removed with the branch itself on 2026-09-22** (item 0.9). Two repairs outlived it and are in the D1b-A branch today: step 1 draws from the source release, not the dada2-format file whose headers carry no `k__` prefix (0 % against 100 %, an all-NA truth table otherwise), and the database is looked up in `db_list` instead of glued by hand. The full diagnosis is in the git history of this file.

### Phase 5 — Biological community (Taudière 2018) (manual actions) — dropped 2026-09-16 (no public data, see D1c)

| #   | Action                                                                                                                                   | Command / location                                                                                                                                              | Blocker for   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 9   | Check data availability in the paper (doi:10.1016/j.funeco.2018.07.008) — locate SRA / Dryad / ENA accession for raw fastqs or OTU table | Manual check of paper's data availability statement                                                                                                             | D1c setup     |
| 10  | Fetch raw fastqs (if available) into `data/data_raw/rawseq_endophyte/`                                                                   | `fastq-dump` / `wget` from accession                                                                                                                            | D1c DADA2 run |
| 11  | Confirm primers match ITS-1F/ITS2; if not, update `config.R` for this run only                                                           | Manual primer check against paper's Methods                                                                                                                     | D1c DADA2 run |
| 12  | Run DADA2 + assignment for biological community                                                                                          | add `dada2_endophyte` / `assign_taxo_endophyte` projects to `_targets.yaml` (same scripts, own stores), then `TAR_PROJECT=dada2_endophyte` and `TAR_PROJECT=assign_taxo_endophyte` `tar_make()` | D1c analysis  |

### D1c — Biological communities (decision 2)

**Dataset:** Taudière et al. 2018, *Fungal Ecology*, [doi:10.1016/j.funeco.2018.07.008](https://doi.org/10.1016/j.funeco.2018.07.008). Tree endophyte ITS metabarcoding with `site` and `height` as sample modalities.

- [x] **Rescoped 2026-09-16 (developer): drop Taudière 2018.** Two targeted searches found no SRA / ENA accession and no Dryad or Zenodo deposit for `doi:10.1016/j.funeco.2018.07.008`. The branch becomes: several **recent** fungal datasets, started from published community objects (phyloseq when available, otherwise an abundance table converted to one), covering **short reads and long reads** and **varied ecological substrates**. The bioinformatic pipeline is explicitly out of scope here, except post-clustering (Q5). Candidates found the same day, none of which advertises a ready-made phyloseq object (they ship fastq plus abundance tables, so a conversion step is needed):
  - **Tedersoo et al. 2026**, *Molecular Ecology Resources*, [doi:10.1111/1755-0998.70189](https://doi.org/10.1111/1755-0998.70189) — 103-species mock **and 45 composite soil samples** sequenced on Illumina MiSeq i100 2×500, PacBio Revio **and** ONT MinION, with several pipelines per platform; code at [`Mycology-Microbiology-Center/fullITS-multiplatform-eval`](https://github.com/Mycology-Microbiology-Center/fullITS-multiplatform-eval). The best single candidate: short and long reads on the *same* samples, plus a mock with known truth.
  - **Graetz et al. 2025**, *Fungal Genetics and Biology* — ONT long-read mock of 54 Dikarya species, fastq on [Zenodo 15751794](https://doi.org/10.5281/zenodo.15751794) (7.1 GB), code at `AbiGraetz/Nanopore-ITS-Benchmarking`. Long-read leg with a known truth.
  - **Tosadori & Bosch 2026**, *Web Ecology* 26:47–59 — in silico ITS1/ITS2 communities of 50 to 800 strains through six pipelines, comparing **ASVs against OTUs**: directly comparable to Q5, scripts on GitHub.
  - **Substrates decided 2026-09-16 (developer): wood, leaf and air**, next to the soil of Tedersoo et al. Root was dropped the same day rather than spend more search on it. Candidates found the same day, with what is actually downloadable:
    - **Air — Global Spore Sampling Project**, Ovaskainen et al., *Scientific Data* 11:561 (2024), [doi:10.1038/s41597-024-03410-0](https://doi.org/10.1038/s41597-024-03410-0). 2 768 samples, 47 outdoor locations, two years, ITS2. **The best candidate by far**: it publishes species-level OTU tables, the taxonomy of each OTU **and the representative sequences**, all as CSV, so it converts to phyloseq with a refseq slot without any reprocessing. **CONFIRMED 2026-09-16** by reading the Data Records section directly (the earlier note repeated the claim without checking it): five CSVs on Zenodo under CC-BY-4.0 — `metadata.csv`, `otu.table.csv` (OTU × sample abundances), `taxonomy.csv` which carries the taxonomy phylum→species **and the sequence of each taxon**, plus `pseudophyla_fungi.csv` / `pseudophyla_nonfungi.csv`. Illumina MiSeq 2×300, ITS2, primers ITS_S2F / ITS3 / ITS4; 27 954 species-level OTUs.
    - **Wood — Korhonen et al. 2024, two Dryad deposits, CONFIRMED 2026-09-16** (file lists and README read directly). Same lab, same primers, same platform, so the two are mutually consistent: [`10.5061/dryad.3j9kd51s7`](https://doi.org/10.5061/dryad.3j9kd51s7) (deciduous fine woody debris, 913 OTUs, 100 replicates over 25 sites plus 20 birch logs) and [`10.5061/dryad.44j0zpcng`](https://doi.org/10.5061/dryad.44j0zpcng) (downed spruce trunks, tops and branches, 31 trees, 243 OTUs). Both ship `Fungi_ITS2_OTU_taxonomy.csv` / `Fungal_OTU_taxonomy_and_metadata.csv` carrying a **`RepSeq` column with the representative ITS2 sequence of every OTU**, plus per-rank taxonomy *with its support values* (`k%`…`s%`) — that is the publication's own inferred taxonomy, exactly the optional second file the developer asked for. Illumina NovaSeq 6000 2×250, ITS2, primers ITS3-2024F / ITS4-2409R. **Caveat that constrains the metrics**: abundances are presence/absence (`44j0zpcng` thresholded at 1 % relative abundance), not counts. Shumskaya et al. 2025, the earlier wood candidate, was not verified and is superseded.
    - **Leaf — open, and the search failed for a reason worth recording (2026-09-16).** Ten-plus candidates were checked file by file and **only one publishes representative sequences**: Karlsson, Friberg, Kolseth, Steinberg & Persson 2017, *Molecular Ecology* 26:3424–3436, [doi:10.1111/mec.14132](https://doi.org/10.1111/mec.14132), data at [`10.5061/dryad.01f10`](https://doi.org/10.5061/dryad.01f10) — `Fungal_OTUs.fasta` (most abundant sequence per OTU, 284 OTUs) + species identification (Table S2) + **quantitative** abundances in ITS copies per ng of wheat DNA (Table S4), the complete triplet. Its only flaw is the date, **2017**, outside the developer's "since 2020" window; that window was instrumental ("there must be plenty"), so this is the developer's call, not an automatic exclusion. Study details confirmed from the Europe PMC record: 22 organically and conventionally cultivated field pairs in Sweden, a mean of 54 OTUs per leaf sample under organic management against 40 under conventional, 284 OTUs in the deposited fasta. **Its ITS region is NOT established**: Wiley returns 403 and the Europe PMC abstract does not name the marker, so two channels were tried and both closed. It is deliberately left unverified rather than inferred from the group's other papers — and since the ITS-region caveat below decides which databases a dataset can be assigned against, this must be settled from the article's Methods before the wheat deposit is adopted. Everything 2020+ was excluded on the same recurring pattern — **processed tables published, sequences left in SRA as raw reads**, which is precisely the reprocessing the developer refuses: Mediterranean phyllosphere (`98sf7m0nh`, table + taxonomy, no sequences), aspen leaves (Zenodo 10839669, script works on `ASV_QIIME` identifiers, no `refseq` / `writeFasta` anywhere), Turco et al. 2026 potato leaf+root (Tables S1–S4 counts and taxonomy, reads at SRA PRJNA1330153), *Betula pendula* foliar+root 2025 (Table S1 indicator species only, reads at PRJNA1211857), *Ageratina adenophora* 2025 (no data-availability statement), *Nothofagus pumilio* 2025 (reads at PRJNA785007; MDPI would not serve the supplement), grapevine phyllosphere 2025 (reads at NCBI), plus three manipulation experiments that are not community surveys. Mukhebi et al. 2024, the earlier leaf candidate, was never verified and is superseded.
    - ~~**Root**~~ — dropped 2026-09-16 (developer): the only study found (PMC12892718) samples root-*zone soil*, not roots, and wood + leaf + air + the Tedersoo soil already give the substrate range the question needs. Reopen only if a mycorrhizal root dataset with a public community object turns up on its own.
    - **Soil and the long-read leg — GloSED, CONFIRMED 2026-09-16** (record read directly), [`10.5281/zenodo.17827890`](https://doi.org/10.5281/zenodo.17827890), CC-BY-4.0, 2026. This is the ideal shape the developer described and it **replaces the rejected Tedersoo soil leg without any reprocessing**: `GloSED__OTU_sequences.fasta.gz` (189.5 MB), `GloSED__OTU_table.tsv.zip`, `GloSED__Taxonomy.tsv.zip` (both also as parquet), `GloSED__BIOM.biom`, `GloSED__SampleMetadata.xlsx` and — decisively — **`GloSED__phyloseq.RData` (265.9 MB), a ready-made phyloseq object**. 988 824 curated OTUs over 4 147 sites in 121 countries, **full-length ITS on PacBio**, so it also supplies the long-read leg. Caveats, both unmeasured and not to be waved away: 988 k query OTUs needs subsampling, and while the D1a measurements show dada2's memory is **reference**-dominated, they say nothing about the **query** side — 988 k queries is k-mer work on a scale this project has never profiled. Measure one subsample before sizing the run.
    - Also worth a look: the **GBIF Australian Microbiome ITS2 terrestrial dataset** ([doi:10.15468/mr93sn](https://doi.org/10.15468/mr93sn)), served as BIOM 2.1, which loads directly.
  - Caveat to check on each: the assignment methods need **sequences**, so a dataset that publishes only an abundance table and a taxonomy, without representative sequences, cannot enter the benchmark.
  - **Second caveat, found 2026-09-16 and it is a scope change to Q2, not a footnote: the ITS region.** This project amplifies **ITS1** — `config.R` sets ITS-1F / ITS2 (Gardes & Bruns 1993 / White 1990, per Pauvert et al. 2018) and `itsx_region <- "ITS1"`. But **every confirmed candidate is ITS2**: Korhonen uses ITS3-2024F / ITS4-2409R, GSSP uses ITS_S2F / ITS3 / ITS4. Only GloSED (full-length ITS) contains the ITS1 and is unaffected. Consequences, read off `config.R:90-102`: the `_Fungi_cut` databases are trimmed at ITS1F and at the reverse complement of ITS2, so they hold **ITS1** — the comment already records that ITS2-only records end up empty or under 50 bp there (317 empty, 1530 < 50 bp in `EUK_ITS_v2.1_Fungi_cut`). An ITS2 query therefore has essentially no overlap with a `_Fungi_cut` reference, and for these datasets **the seven-database grid collapses to the untrimmed variants** (the full releases and `_Fungi`, whose UNITE / EUKARYOME records span the whole ITS). The ITSx leg of Q4, which keeps ITS1 only, does not apply to them either. **Resolved 2026-09-16 (developer): ITS2 becomes an axis of the benchmark, not a limitation.** Working on ITS2 data is wanted ("c'est même très intéressant de le faire"), so the path chosen is to **derive `_ITS2_cut` database variants alongside the existing `_Fungi_cut` ones** — `make_databases.R` already has the machinery and only the primer pair changes (ITS3 / ITS4 instead of ITS-1F / ITS2). The reduced-grid and full-length-only options are dropped. Consequence to keep in view: `config.R::primer_min_overlap` is defined as `nchar(rev_primer_sequences)` and the `_cut` derivation reads `cut_discard_untrimmed` / `cut_min_length`, so an ITS2 variant needs its own consistent set of primer-derived constants, not a partial override.
- [ ] `[Medium / moderate]` Decide which of these datasets enter the manuscript, and write the loader that turns their abundance tables into phyloseq objects with a refseq slot (the assignment methods all need sequences, not just an OTU table).
- **Scope reopened 2026-09-16 (developer), and it changes the exclusion criterion.** Reprocessing raw fastq from SRA/ENA is now in scope, with a script close to `pipelines/dada2.R` minus the `clusterize` post-clustering step, so **every leaf dataset excluded above for "sequences only as raw reads" is back in play**. Long reads go through [NextITS](https://next-its.github.io/). Abundances are no longer needed: the biological communities serve to compare the **number of NA in the taxonomy** against real data, not to redo ecological analyses — which also retires the presence/absence and Bray-Curtis caveats on the Korhonen deposits.
  - **Tedersoo et al. 2026 is back in**, complementing the Pauvert mock: Illumina mocks through a dada2-style script, long reads through NextITS plus the scripts of `Mycology-Microbiology-Center/fullITS-multiplatform-eval`. Raw data at ENA **PRJEB108994**. **Measured 2026-09-16 from the ENA run report**: the Illumina mock is exactly two runs, `ERR16773638` (`ILLUvs__MOCK`) and `ERR16773639` (`ILLUvs__MOCK_2`), iSeq 100, PAIRED, ~495 bp per read — so a ~600 bp full-length ITS amplicon merges with ~390 bp of overlap and the dada2 route is viable. Two caveats: ITS length varies (400 to 800+ bp), so amplicons over ~990 bp will not merge and long-ITS taxa drop out, a bias to state in the Methods; and 3'-end quality at 495 bp on iSeq makes the `filter_trim` truncation settings decisive. **Fastq downloaded and checked 2026-09-16** into `data/data_raw/rawseq_tedersoo_illumina/` (19.7 MB, four files): the four MD5 sums match the ENA record, kept next to them in `ena_md5.txt`. Note for anyone repeating this: EBI's **FTP endpoint fails** on these paths (`CWD` reports the directory missing) while the **HTTPS** one serves them correctly — use `https://ftp.sra.ebi.ac.uk/vol1/fastq/...`, and take the paths from the API's `fastq_ftp` field rather than deriving them, since the subdirectory is the accession's last two digits padded to three (`ERR16773638` → `038`). Note the deliberate divergence from the paper, which processed its own Illumina data with NextITS, not dada2.
  - **NextITS output shape VERIFIED 2026-09-16 from the source** (the documentation site has no output page — `/output/` and `/results/` both 404 and the README lists nothing — so the answer was read from the pipeline code, which is the authority anyway). `workflows/STEP2.nf:568-574` and `:630-636` publish, per run: `OTU_table_wide.txt.gz`, `OTU_table_long.txt.gz`, **`OTU_table_wide.RData` / `OTU_table_long.RData`** (loadable R objects), **`OTUs.fa.gz`** (the representative sequences) and `UC_Pooled.parquet`; with LULU curation (`:700-703`) it also writes `OTU_table_LULU.txt.gz`, `OTUs_LULU.fa.gz`, the match list and the merging statistics. So the long-read loader needs no reconstruction: table plus representative sequences plus a `.uc` join key are all published, and a phyloseq with its refseq slot follows directly. Convenient detail: NextITS runs LULU through **MUMU**, the same implementation as `MiscMetabar::mumu_pq()` already used by `pipelines/dada2.R`, so the post-clustering step stays comparable between the short-read and long-read legs. The README does confirm Illumina is supported, which leaves a consistency option with the paper. Nanopore in that study went through Minovar / PRONAME, not NextITS, so the ONT leg is not simply "NextITS".
  - **Design gain found 2026-09-16 in the NextITS source: the long-read leg yields ITS1 and ITS2 from the same reads.** `workflows/STEP1.nf:716-729` and `subworkflows/itsx_subworkflow.nf:157-172` publish, per sample and side by side, `.full`, `.SSU`, `.ITS1`, `.5_8S`, `.ITS2` and `.LSU` fasta (plus `full_and_partial` variants and `_no_detections`), under `${params.outdir}/03_ITSx` — `--its_region` selects what is carried downstream, not what ITSx extracts. Consequence for the manuscript: on the Tedersoo PacBio samples the **ITS region becomes a within-dataset contrast rather than a between-dataset confound**. The same biological samples can be assigned against the ITS1 grid (`_Fungi_cut`) and the ITS2 grid (`_ITS2_cut`, to be derived), which isolates the effect of the marker from the effect of the community — something the Illumina legs, each locked to one region, cannot do.
  - **Architecture decided 2026-09-16 (developer): one project pair per dataset.** `dada2_<dataset>` (script `pipelines/dada2_bio.R`, own store) then `assign_taxo_<dataset>`. Implemented the same day: `config.R::bio_datasets` holds the raw folder, metadata file, primers and fastq name patterns of each dataset, `bio_dataset` / `bio_cfg` derive from `TAR_PROJECT` exactly as `mini_db` does, and `dada2_store` feeds the now-parameterised `d_asv_file` of `pipelines/assign_taxo.R` — so the production projects and the target names pinned by `tests/test_values_map.R` are untouched. `pipelines/dada2_bio.R` drops the `clusterize` branch (target `d_idtaxa` in `pipelines/dada2.R`, misleadingly named since it calls `method = "clusterize"`) and keeps `d_vs`, which Q5 rests on. The ENA names files `_1` / `_2` where the mock fastqs use `_R1` / `_R2`, hence the per-dataset patterns. **Defect found and fixed 2026-09-16, first run of the project**: the patterns were passed to `list_fastq_files()` but not to `cutadapt_remove_primers()`, which carries its own `pattern_R1` / `pattern_R2` (defaults `_R1` / `_R2`) and relays them to `list_fastq_files()` itself — so cutadapt ran first, matched nothing and stopped with *"None file in the folder … match the pattern_R1 _R1"*, recorded in `store_dada2_tedersoo_illumina/meta/meta`. Parameterising the second call while leaving the first on its defaults is the trap: check every function in the chain that takes a file pattern, not just the one that reads the folder last. **Second act, same day**: with the plumbing fixed the error moved from `pattern_R1 _R1` to `pattern_R1 _1`, and a directory listing showed why — the files had meanwhile been renamed on disk to `ERR16773638_R1.fastq.gz` / `_R2` (they were `_1` / `_2` when downloaded and MD5-checked at 15:18), aligning them with the mock-community convention. `bio_datasets` now declares `_R1` / `_R2` for this dataset. Lesson recorded because it cost two rounds: **list the directory before trusting the naming a repository's metadata implies**, and keep the pattern plumbing parameterised anyway, since the next dataset will not follow this convention either.
  - **[x] The Tedersoo Illumina pipeline runs end to end (2026-09-16), after the developer rebuilt `cutadaptenv`.** Environment now **cutadapt 4.9 / Python 3.12.14** — the same cutadapt that produced the mock community, to record in the Methods. Result: **238 ASVs over 2 samples, 17 016 reads**, taxonomy filled at 100 % (Kingdom), 89.9 % (Genus), 82.4 % (Species) by the seed assignment. Sequence lengths 201–908 bp, **median 716**, which confirms the full-length ITS amplicon really merged from the 2×495 bp reads, as predicted from the ~390 bp overlap; `seq_len_min = 200` barely bites. Post-clustering: 180 vsearch OTUs, 142 / 122 after MUMU. The downstream project is validated too — `assign_taxo_tedersoo_illumina` resolves to `store_dada2_tedersoo_illumina`, finds `d_asv` and builds its 207 targets.
  - **[Medium / moderate] Read retention is lopsided between the two mock runs, and the cause is identified.** `track_df`: 86 315 raw forward reads → 23 628 after primer removal (**72.6 % lost**), then 21 557 denoised → 17 158 paired → 17 016 after chimera. The loss is not uniform: `ERR16773638` keeps 53.7 % of its pairs, `ERR16773639` only 15.3 %. Per-adapter counts from the cutadapt JSON reports locate it exactly — on `ERR16773639` the **5' reverse primer (ITS4ngsUni, the `-G` adapter) is found in 8 861 of 59 220 R2 reads (15 %)** against 29 055 (49 %) for the forward primer on R1, while `ERR16773638` is symmetric (14 686 vs 14 675). So it is neither 3' read-through nor end-of-read quality, but the very start of R2 in that run, and it is not a parameter problem: the same primers behave perfectly on the other sample of the same deposit. Consequence: the larger run contributes fewer reads (9 087) than the smaller one (14 541). Options if it matters for the mock metrics: accept it, relax `-e` / `--discard-untrimmed` for this dataset only, or inspect that run's R2 quality directly.
  - **Amplicon-specific `_cut` databases (developer decision 2026-09-16), and why they serve the manuscript's central question.** The rule: **a `_cut` reference is trimmed with exactly the primers of the dataset assigned against it** — ITS1F / ITS2 for the Pauvert mock (the existing `_Fungi_cut`), ITS9mun / ITS4ngsUni for Tedersoo (`_Fungi_cut_full_ITS`), others decided as they arrive. Implemented as a *swap*, not an addition: `config.R::bio_datasets$cut_suffix` drives a substitution of the cut entry in `db_list`, `itsx_db_list` and `db_meta$db` when a dataset project is active, so the grid keeps its shape and size, every other target name is untouched, and neither the mock grid nor the CV pays anything. `make_databases.R` step 2b-bis derives one variant per dataset and per source with that dataset's own primers and `min_overlap = nchar(its reverse primer)`, plus the matching `mini_*`. Why it matters beyond correctness: the benchmark asks which **method × database (including database modifications) × parameter** combinations best trade assignment quality against compute time and memory, and trimming is the main lever on the cost side — dada2 needs ≥ 45 GB on the full `EUK_ITS_v2.1` (D1a measurements) where the trimmed variant holds 1 075 023 records. Trimming per amplicon also removes a confound that would otherwise make the comparison dishonest: a database was being penalised for a region the queries never cover. Costs come out automatically, since every compute target is wrapped in `with_autometric()` and `benchmark_costs` aggregates on `compute_name`, which carries method and database.
  - **Pre-flight checks before the first `assign_taxo_<dataset>` run (2026-09-16), all three passed or are understood.** (i) **Negative controls work on the real object**: `add_shuffle_seq_pq()` on `d_asv` (238 taxa, 4 sample variables) adds 119 `fake_` taxa at `prop_fake = 0.5`, then `add_external_seq_pq()` adds the 100 `external_` ones — 457 taxa of which **219 controls**, so the TN denominator of `tc_metrics_mock()` exists and the two-sample object is not a problem. (ii) **ITSx environment healthy**: ITSx 1.1.3, hmmer 3.4, perl 5.32.1 in `itsxenv` — checked because the cutadapt regression showed a conda env can rot unnoticed. (iii) **`taxo_mock` is a trap rather than a blocker**: `pipelines/assign_taxo.R:140-157` builds it from the global `config.R::taxo_mock_csv`, which points at the **Pauvert** mock, and nothing downstream in that script consumes it. So a dataset run does not break, but it **stores Pauvert's truth table inside the dataset's own store**, where a later reader would take it for that dataset's truth. Either parameterise `taxo_mock_csv` per dataset (NA when a dataset has no truth table) or document it loudly. Tedersoo's own truth (103 species) is not in the project yet: the paper puts its mock specimens in UNITE, Table S1.
  - **[Critical / easy] `cutadaptenv` had regressed to cutadapt 2.6 and it broke primer removal silently (found and fixed 2026-09-16).** The installed package is `cutadapt 2.6 py37h516909a_0` (a Python 3.7 build), while the mock community was processed with **4.9** — the version is recorded in `data/data_intermediate/seq_wo_primers/ITSEnz_DikA_R1.cutadapt.json`. `MiscMetabar::cutadapt_remove_primers()` always builds a `--json=` argument, an option introduced in cutadapt **3.5** (2021-09-29, current stable 5.2), so on 2.6 every command dies with *"unrecognized arguments: --json"* and no output file is written. **Fix: reinstall cutadapt >= 4.9 in `cutadaptenv`** (developer's call, the env is shared by `dada2`, `dada2_bio` and the CV query trimming).
  - **[High / moderate] The failure above is silent, which is the more serious defect.** `tar_make()` reported `✔ cutadapt completed [1.1s, 0 B]` although both cutadapt calls had failed: the target is `format = "file"` on the *output folder*, and `cutadapt_remove_primers()` creates that folder with `dir.create()` as its first statement, so the path always exists and targets validates the target whatever happened inside. The pipeline then breaks at `data_raw`, one step later and far from the cause. `pipelines/dada2.R` carries exactly the same pattern. Worth a guard that fails when the folder holds no fastq, in both scripts.
  - **[Critical / moderate] The cutadapt regression reaches beyond D1c, and the dates say so.** Binary `cutadaptenv/bin/cutadapt` dates from **2026-08-21** (the 2.6 downgrade); `EUK_ITS_v2.1_Fungi_cut.fasta` and its `mini_` variant from **2026-09-15**, i.e. **derived three weeks after the regression, with cutadapt 2.6**; the mock's 4.9 report from 2024-12-19, long before. `dbpq::cutadapt_rm_primers_db()` defaults to `output_json = FALSE`, so it never passes `--json` and does **not** fail on 2.6 — it simply runs a 2019 binary without saying so. Consequences to settle before touching the environment: (i) the `_Fungi_cut` databases currently in the stores, and the figures recorded for them (317 empty records, 1530 under 50 bp), were produced by cutadapt 2.6; (ii) CV query trimming goes through the same function, so a cross-validation launched now also runs on 2.6; (iii) a feared semantic divergence on upgrade was **tested and not reproduced**, so the upgrade looks safe. The worry came from the changelog: cutadapt **3.0 (2020-11-10)** stopped anchoring linked adapters (`-a X...Y`) by default, and neither `make_databases.R:372` nor `R/cv_queries.R:39` sets `start_with_fw`, so no `^` is ever added — on paper the same command would trim differently before and after 3.0. Run against the installed 2.6 on a record carrying ten bases of junk ahead of the forward primer, the command shape the project actually uses (`-a 'FW;optional...RC(REV);optional' --discard-untrimmed -e 0.1`) trimmed junk and both primers and wrote the read: **2.6 does not anchor here either**, presumably because the `;optional` modifier settles the interpretation. Limits of that evidence, stated so nobody over-reads it: one record shape on one binary, and 2.6 could not be compared against 4.9 without installing 4.9. So **do not re-derive the `_Fungi_cut` databases or rerun the CV merely because cutadapt is upgraded** — that would be hours of compute on an untested premise. Still record the cutadapt version used for the final production run in the Methods, and if certainty is wanted, re-derive one `mini_*_Fungi_cut` after the upgrade and diff it against the stored file.
  - **Primers VERIFIED 2026-09-16** against `primers.fas` of the study's own repository, downloaded raw (62 bytes): `>ITS9mun_ITS4ngsUni` / `GTACACACCGCCCGTCG...GCATATHANTAAGSGSAGGcg`. This is cutadapt's linked-adapter form, forward then reverse-complemented reverse, so the pair is ITS9mun `GTACACACCGCCCGTCG` and ITS4ngsUni `CGCCTSCSCTTANTDATATGC` (stored in its reading sense in `config.R::bio_datasets`). **The NextITS documentation's default values are truncated** — they drop the leading `G` of ITS9mun and the leading `CG` of ITS4ngsUni — and the first version of `bio_datasets` had copied them; corrected the same day. Never take these primers from the NextITS docs.
  - **Negative controls on the Tedersoo mock: yes** (developer, 2026-09-16), with a second sample variable so `add_shuffle_seq_pq()` works on a 2-sample object — without the `fake_` / `external_` taxa there is no TN denominator, so TNR, MCC and ACC would be lost and the comparison with the Pauvert mock would break. The metadata CSV `data/data_raw/metadata/sam_data_tedersoo_illumina.csv` therefore carries `platform` and `community` next to `Sample_names`.
  - **GloSED: ~10 sites stratified by biome** (developer, 2026-09-16), from `GloSED__SampleMetadata.xlsx`, rather than assigning close to a million OTUs. The resulting OTU count is to be **measured, not assumed**.
- [ ] `[Low / easy]` comparpq robustness, found while repairing the in silico notebook (2026-09-16): `add_shuffle_seq_pq()` silently requires a `tax_table` **and** a `sample_data` with at least two variables. Reproduced on a 6-taxon object: without a tax_table it fails in `as.matrix(unclass(new_physeq@tax_table))`, and with a single sample variable `merge_phyloseq()` drops the table to a vector, so phyloseq reports the misleading "sam_data slot is empty". Two guards with actionable messages would save the next caller the same hunt.
**The four items below are dropped** (2026-09-16, decision 26 and the D1c rescoping just above): no public data for Taudière 2018, and the biological communities are now read through `NA_real`, `NA_fake` and `ext_fungi` only. Ticked 2026-09-22 at the ROADMAP split so they do not read as open work.

- [x] If raw fastqs are available: drop them in `data/data_raw/rawseq_endophyte/` and run a copy of `script_dada2.R` with adapted primer / sample metadata. Stand up `store_dada2_endophyte` parallel to `store_dada2`.
- [x] Confirm primers match the Pauvert ITS-1F/ITS2 pair — if not, update `fw_primer_sequences` / `rev_primer_sequences` in `config.R` *for this run only* (do not overwrite the canonical values).
- [x] Run the assignment loop on `d_asv_endophyte`. No `tc_metrics_mock` (no ground truth).
- [x] Agreement-only metrics: Jaccard / Bray-Curtis between method-pairs, per-sample richness comparison, consensus-vs-single calls. Decompose by `site` and `height`. **Constrained by the datasets chosen (2026-09-16)**: the two Korhonen wood deposits are presence/absence, one of them thresholded at 1 % relative abundance, so **Jaccard survives, Bray-Curtis does not** (it needs counts) and per-sample richness is biased by the threshold. The per-taxon method-agreement metrics — which are the benchmark's actual core, since every method reassigns the same sequences — are unaffected. GSSP, GloSED and the 2017 wheat deposit all carry real abundances, so the full metric set applies there. One thing to settle before writing the loader rather than discover inside `add_shuffle_seq_pq()`: `tc_metrics_mock()` draws its TN denominator from the injected `fake_` / `external_` controls, and injecting controls into a presence/absence object requires choosing an abundance convention for them (1 everywhere, or the dataset's own convention).
- [ ] **Methods-section text**: this is the "real conditions" leg of the four-data approach; emphasize that disagreement here is informative even without truth.

### M1 — Performance metrics (remaining)

- [ ] **M1.2** — Add Edgar-style **misclassification rate / over-classification rate / EPQ** as alternative columns to `tc_metrics_mock` output, only for cross-validation datasets where "novel" vs "known" is well-defined.
- [ ] **[Medium / moderate]** Finalize distance-to-true-community metric (currently the "En chantier" section of `benchmark.qmd`, lines 1077–1200; moves to `analysis/sandbox/` with S3.3). (source: `analysis/benchmark.qmd`:1077)
- [x] **M1.4** `[Critical / moderate]` **Implemented 2026-09-22 as items 0.6 and 0.7** (`R/mock_truth.R`, `comparpq::tc_metrics_unit()`); ticked at the ROADMAP split. Per-sequence truth and Hleap scoring (developer, 2026-09-17: "We must take the same approach"). Build one truth lineage per ASV (and per OTU) of each mock as Hleap et al. 2021 did: BLAST against the Sanger sequences of the mock members (≥ 99.5 % identity, 100 % query cover), then curation of the rest. Then score each sequence in one cell per rank as `TA_pipes::optimize_n_score.py::score()` does (real = own label → TP; real, other taxon → FP; real, NA → FN; shuffled, any taxon → FP; shuffled, NA → TN). Pauvert: Table S1 with the 189 Sanger sequences stored in `data/data_raw/metadata/pauvert2019_table_s1_mock_sanger.csv` (2026-09-17). Tedersoo: Table S1 of doi:10.1111/1755-0998.70189 (103 specimens, Sanger reads, binomial names only) downloaded by the developer and stored as `data/data_raw/metadata/tedersoo_mock_table_s1.xlsx` and `tedersoo_mock_table_s1_sanger.csv`. Measured 2026-09-17 against the Sanger sequences: at ≥ 99.5 % identity 150 of 196 Pauvert ASVs (105 with 100 % query cover) and 115 of 238 Tedersoo ASVs (none with 100 % cover: the ASVs are longer than the Sanger reads) get a truth; 98–99 % change little. Decided: units without truth stay in the matrix (FN if NA, FP otherwise) and in `NA_real`; ties take the lowest common rank; TNR = TN / (TN + FP). Matching rule confirmed by the developer (Q2a): the shorter of ASV and Sanger sequence is covered at ≥ 99.5 % with ≥ 99.5 % identity — 149 of 196 Pauvert ASVs (95.6 % of reads) and 99 of 238 Tedersoo ASVs (87.6 % of reads) get a truth; the 105 Tedersoo ASVs without any hit ≥ 90 % hold 7.3 % of the reads (median 3 reads) and are discussed in the Tedersoo paper, not investigated here. Rebuild the Tedersoo lineage (binomials only) with the GNA verifier, as for Pauvert (`taxinfo::gna_verifier_pq(data_sources = 11, classification_col = TRUE)`, option added 2026-09-17). Decided: truth = submitted name, and the submitted or the GBIF accepted name both count as correct at Species and Genus (Pauvert species found in UNITE_sh rise from 134 to 153); ranks above Genus from the GBIF lineage for both mocks, Table S1 disagreements checked by hand (`docs/hleap_2021_metrics.md` §6) and align both truths with the database names (species found: Pauvert 134 / 181 in UNITE_sh, 148 in EUKARYOME; Tedersoo 71 / 103 and 94). TN counted per rank as in the code; TNR and `prop_fake = 0.5` kept. → `docs/hleap_2021_metrics.md` §4–5
- [x] **M1.5** `[Medium / easy]` (done 2026-09-22, ROADMAP 0.8b) Abundance-filter sweep (developer, 2026-09-17): recompute the mock metrics after removing units with fewer than *n* reads, *n* ∈ {2, …, 10, 12, 15, 20, 30, 40, 50, 100}, and show how each metric changes, **on the mock communities and the biological datasets only** (not on cross-validation nor in silico). Post-hoc on stored assignment columns, almost free. Hleap et al. 2021 removed ASVs below 8 reads before assignment. → `docs/hleap_2021_metrics.md` §4 point 6
- [ ] **M1.6** `[Low / easy]` Discussion note: `comparpq::add_shuffle_seq_pq()` shuffles single nucleotides (`sample()`), Hleap et al. 2021 used `esl-shuffle` preserving di-nucleotide composition; mono-nucleotide shuffles likely make the negative controls easier to reject for k-mer methods (sintax, dada2), so TN may be higher than with Hleap's controls. → `docs/hleap_2021_metrics.md` §4 point 5

### M2 — Environmental metrics (reopened)

- [ ] **M2.1 / M2.2 / M2.3 / Q2.5 re-validation** — S1.1 is fixed and verified on the mini run (72 cost rows, 54 measured; sintax targets on mini databases end before the first 1 s sample). The existing figures still come from the 2025-02-11 log and from the invalid 2026-05 stores: regenerate with S6.5.

### Cross-cutting / housekeeping (remaining)

- [ ] **C1** — Run `make_databases.R::derive_all_variants()` end-to-end on a clean machine to verify the dbpq-delegated derivations match the legacy outputs byte-for-byte (or close enough). Use a test directory to avoid clobbering current DBs. (Phase 3 verified the *current* machine; C1 is the clean-machine check.)
- [ ] **C4** — Consider proposing `combine_taxo_assignments` and `cross_val` to `comparpq` (noted in `proposals_for_dbpq.md` as not-for-dbpq). Out of scope for this benchmark; nice for the broader pqverse.
- [ ] **[Low / easy]** Optionally add "fake" taxa to benchmark for TRUE-negative / trade-off analysis. Not urgent. (source: `R/functions.R`:103 — note: that file is 50 lines; the TODO now lives in `R/cross_val.R`:22.)

---

## 3. Decisions taken (2026-05-19)

1. **IdTaxa** — *not* included in the benchmark. The methods set is fixed at four: dada2, sintax, lca, blastn. The manuscript needs a one-line justification (e.g. "IdTaxa requires a separate training step and is therefore excluded from this cost-aware benchmark; see [reference] for an IdTaxa-focused comparison").
2. **Biological dataset** — endophyte dataset from **[Taudière et al. 2018, Fungal Ecology, doi:10.1016/j.funeco.2018.07.008](https://doi.org/10.1016/j.funeco.2018.07.008)**, using `site` and `height` as sample modalities.
3. **In silico tooling** — both **InSilicoSeq** (for fastq simulation from a curated fasta) **and miaSim** (for community-level structure from neutral/ecological processes). D1b has two sub-branches.
4. **Mini DBs in publication** — dropped. `mini_*` derivations stay in `make_databases.R` purely as a smoke-test tool for fast iteration on the analysis; they are excluded from the Results.
5. **Primary metric set** — **F1 + MCC** (Hleap-style) **+ TAR/TDR per rank** for mock-community results. F1 is the headline; MCC complements it (handles class imbalance); TAR/TDR are presence/absence checks for the mock.
6. **`nb_agree_threshold` values** — **1, 2, 3** (as default in Q3.3).
7. **SSU database reduction** — of the four EUK_SSU variants (full, Fungi-only, cut, Fungi+cut), only **EUK_SSU_v2_Fungi_cut** (named `EUK_SSU_v1_9_3_Fungi_cut` at decision time) is retained. Rationale: the ITS region is a subregion of the 18S SSU gene, so ITS sequences are already present in any full SSU database — comparing all SSU variants would be redundant with the ITS database comparisons. The Fungi-filtered, primer-trimmed variant is the most directly comparable to the ITS databases and avoids inflating the DB axis with near-duplicate conditions. **Revised 2026-09-15 (developer): no SSU database.** The rationale above is wrong: ITS1 is not part of the 18S gene, and trimming an SSU record at ITS1F leaves the 46 bp of 18S that start the ASVs (S6.9, B21).

## 3b. Decisions taken (2026-09-10)

8. **Figures are not versioned** — `figures/` is gitignored (only `.gitkeep`); the manuscript folder holds the reference copies.
9. **targets meta stays in git** — `store_*/meta/meta` remain tracked for provenance; `tar_prune()` runs after every `tar_make()`.
10. **Reference code = dev checkouts** — MiscMetabar, comparpq, dbpq, greenAlgoR and tidypq are loaded from `pqverse_pkg/` (`PQVERSE_PKG_DIR`, S2.2), not from CRAN; a `session_info` target pins the versions used for the manuscript.
11. **`41576_2023_679_MOESM2_ESM.ods` kept** in `docs/references/`, source to be documented.
12. **Second mock community SRR30413326** joins the benchmark as dataset D1d; the Hleap 2021 fish mocks await a separate decision.
13. **tidypq adopted in the notebook and the pipelines** (S5.1, S5.2): phyloseq slots are no longer manipulated by hand where a tidypq verb exists; `pq_to_tidy()` replaces `psmelt()`.
14. **Reference releases (2026-09-11)** — UNITE 19.02.2025 and EUKARYOME v2.1, obtained from the providers' general FASTA releases (`config.R::reference_sources`) and converted locally to dada2 and sintax formats.
15. **Versioned database names (2026-09-11)** — every source and derived database carries its release (`Unite_s_all_20250219`, `EUK_ITS_v2.1_Fungi_cut`, …): a new release creates new targets, and two releases can be benchmarked side by side.
16. **dbpq downloads UNITE by URL (2026-09-11)** — `url` and `extract` were added to `dbpq::download_unite_db()` rather than writing a local download wrapper.
17. **UNITE without singletons benchmarked (2026-09-11)** — `Unite_all_20250219` and its Fungi variant join the database axis next to the release with singletons, which answers the side question on singletons.
18. **blastn uses `min_cover = 80` (2026-09-11)** — first kept at the `assign_blastn()` default of 95, then revised the same day once B18 showed that the ASVs start with 30–45 bp of 18S absent from ITS references (156 of 200 best hits on the full UNITE Fungi database below 95 % query cover). Set in `config.R::blastn_min_cover`, used by the assignment and cross-validation pipelines. Whether ITS extraction with ITSx improves assignment becomes a new question (Q4).
19. **ITSx axis (2026-09-11)** — ITSx is used to remove the flanking regions, not as a Fungi filter (all organism profiles, no sequence dropped). ITSx 1.1.3 extracts ITS1 only from `d_asv`, before the negative controls (same shuffled ASVs, same external sequences); every method × database × parameter runs on both inputs, in `store_assign_taxo`, with `itsx_`-prefixed target names (Q4). Revised 2026-09-14 (S8.5, lever L7): the ITSx input is assigned against the Fungi-filtered databases only (`config.R::itsx_db_list`).
20. **ASVs identical after ITSx (2026-09-11)** — when several ASVs share the same ITS1 once trimmed, the most abundant is kept and the others are removed from both inputs (`itsx_duplicated_taxa()`, target `d_asv_common`), so the raw and ITSx inputs keep the same taxa and the same controls. The rule applies to every dataset (mock, in silico, biological). On the mock community: `Taxa_146` (52 reads) shares its ITS1 with `Taxa_12` (2 291 reads), so both inputs hold 199 ASVs. The 200 ASVs of the mock are not a cut-off: DADA2 gives 222 ASVs, 204 without chimeras, 200 of at least `seq_len_min` = 200 bp.
21. **Kingdom rank kept (2026-09-15)** — Kingdom stays among the evaluated ranks. On the mock the truth holds a single value (Fungi), so the MCC is often degenerate there (80 zero-margin Kingdom cells on the mini store, S6.8): report TAR / TDR next to it.
22. **Full article on four datasets (2026-09-15)** — the manuscript covers the mock community, cross-validation (D1a), in silico simulations (D1b) and the biological community (D1c), rather than a shorter note on the mock and cross-validation. D1d (second mock, decision 12) is unchanged.
23. **Reference trimming keeps untrimmed records (2026-09-15)** — the `_Fungi_cut` databases are trimmed at ITS1F when found and at the reverse complement of ITS2 when found; records without primer sites are kept whole (`config.R::cut_discard_untrimmed = FALSE`; dbpq keeps `discard_untrimmed = TRUE` as its default). dbpq reverse-complements the reverse primer by default, with `Biostrings::reverseComplement()` rather than `dada2::rc()` (dada2 is only a Suggests of dbpq) (S6.9).
24. **Cross-validation queries trimmed to the amplicon (2026-09-15)** — only the test sequences go through cutadapt (`R/cv_queries.R`); records without the ITS2 site are never queried but stay in the training part, which keeps full-length records; `cv_oversample = 2` records are drawn per wanted query so that every database gets `cv_max_seq` queries (D1a). A `_Fungi_cut` database, whose records no longer hold the primer sites, gets its queries from its `_Fungi` source, trimmed the same way and paired by name with the `_cut` records of the training part.
25. **OTU taxonomy from member ASVs (2026-09-15)** — Q5 compares the ASVs with 97 % vsearch OTUs whose taxonomy, per assignment column, keeps a value only when all member ASVs assigned at that rank agree (`MiscMetabar::resolve_vector_ranks(method = "unanimity")`, NA ignored). No assignment is recomputed and the negative controls are not clustered (Q5).

## 3c. Decisions taken (2026-09-17)

Full text, verbatim answers and consequences: `docs/objectives_design.md` §4 (its decisions 1–27, "OD N") and `docs/hleap_2021_metrics.md` §5–6. Only what changes a decision above is listed here.

26. **Scope: Fungi taxonomic assignment only (OD 12)** — no community ecology. **Supersedes decision 3** (miaSim dropped; D1b = InSilicoSeq, source `Unite_s_all_20250219`, OD 5) and narrows decision 22: biological datasets are read through `NA_real`, `NA_fake` and `ext_fungi` only.
27. **Database grid (OD 1, 3, 13, 14, 21, 24, 25)** — 3 sources (`EUK_ITS_v2.1`, `Unite_all_20250219`, `Unite_s_all_20250219`) × 3 simplifications (all, fungi, fungi + 5 % non-fungal reps). **Supersedes decision 23 for the grid**: no `_cut` database; trimming returns as a final zoom on the best combinations, Pauvert only. Decision 23 still describes how `_Fungi_cut` is built for that zoom.
28. **OTUs assigned directly (OD 4, 19)** — `d_vs` gets its own assignments and controls, no MUMU, no ITSx. **Supersedes decision 25.**
29. **ITSx on fungi and fungi + rep databases (OD 19, S8.5)** — decision 19 unchanged otherwise; needs a per-dataset `itsx_region`.
30. **Parameters (OD 9, 10, 17, 18, 23)** — lca `lca_cutoff` ∈ {0.8, 0.9, 1} derived in MiscMetabar from a stored hit table; blastn `min_id` ∈ {90, 92, 95, 97} derived from the stored hits; `min_cover = 80` unchanged (decision 18).
31. **Per-unit truth and Hleap scoring (hleap §5–6, OD 6, 7, 15, 22)** — **revises decision 5**: one truth per unit from the Sanger sequences (identity ≥ 99.5 %, shorter sequence covered ≥ 99.5 %), one confusion-matrix cell per unit and rank, TNR = TN / (TN + FP), `external_` in the matrix on fungi databases only; F1 and MCC stay the headline within a database simplification, PPV / TPR / F1 / `NA_real` / control rates across simplifications. Parameters are reported on the whole mock, not tuned on a held-out third.
32. **Controls (OD 26)** — `external_` drawn from the 13 non-fungal kingdoms retained in the three releases (done 2026-09-17); `prop_fake = 0.5` kept.
33. **Cross-validation databases (OD 20, 27)** — fungal records queried only; database subset chosen after the mock results.
34. **Hleap 2021 COI mocks deleted (2026-09-17)** — closes the open part of decision 12.

## 3d. Decisions taken (2026-09-22)

Settled while implementing the grid and while answering the critique of
2026-09-22. The framing decisions 37–39 are the developer's own words, recorded
in the addendum of [`critique_2026-09-22.md`](critique_2026-09-22.md) and in
[`literature_2026-09-22.md`](literature_2026-09-22.md) §4.

35. **A `fungi + rep` representative stays below 97 % identity with every `external_` control** (`config.R::rep_max_identity_to_external`, item 0.2b) — the rule applies to the representatives only: the whole releases contain the controls by construction (they are records of `fake_ref_source`, which is what makes `ext_correct` computable there) and the `_Fungi` databases contain none.
36. **The Q3 consensus votes over the default settings only** (item 0.4d) — one column per method and database, 4 × 9 = 36 at equal weight, instead of the 189 columns of the full sweep where blastn alone would carry 57 % of the vote. **Revised in spirit by decision 39**: equal weight over 36 columns is still a vote over *correlated* voters.
37. **Orsholm et al. 2026 is the foundation, not a competitor (framing)** — for fungal ITS the algorithm-family question is settled: composition-based classifiers beat phylogenetic placement and generic similarity search. The four-method set is therefore a **deliberate zoom**, not a gap, and the paper must say so and cite it. Honest nuance to keep in the writing: **lca is search-based like blastn**, so the zoom is 2 composition-based (dada2, sintax) × 2 similarity-search (lca, blastn), with blastn as the abstaining contrast. **Narrows decision 1**, whose "IdTaxa excluded on cost-parity grounds" becomes a second-order argument behind this one. → `literature_2026-09-22.md` §4.1
38. **Database composition and voting are the two complexity layers (framing)** — the paper puts its complexity where the literature has none: `[EUKARYOME | UNITE ± singletons] × [all | Fungi | Fungi + rep]`, and whether a vote over several classifications beats the best single one. **Q3 is a primary axis**, which withdraws the critique's proposal to demote it (C22-P5 demotes Q5 only). → `literature_2026-09-22.md` §4.2–4.3
39. **Consequence the developer accepted with decision 38: C22-P6 is required, not optional** — a vote over columns that include the nested UNITE pair (`Unite_all` ⊂ `Unite_s_all`) and two search-based methods reading the same reference counts shared evidence twice, so the Q3 figures cannot support a claim about voting until the gain is reported against the effective number of independent voters, and the subset analysis of C22-P21 is run.
40. **One language, one dependency stack, and the declaration of interests that follows (framing)** — the four assigners (MiscMetabar), the consensus strategies and the per-unit scoring (comparpq) all live in the pqverse, which no competing benchmark can claim. Its flip side is an author overlap between the harness and part of the benchmarked software: declare it, pin the package commits per run (the `session_info` targets), and state that the engines (vsearch, BLAST+, dada2) are independent third-party tools (C22-N1). → `literature_2026-09-22.md` §4.4
41. **The ROADMAP is split into a todo and this archive (C22-P19)** — `ROADMAP.md` keeps one line per open item, `HISTORY.md` keeps every ticked item, dated narrative and past decision. New decisions are written in `docs/objectives_design.md` §4 (OD 28, 29, …) and summarised here, never stated for the first time in `ROADMAP.md`.
42. **Left open on 2026-09-22 (developer): the primary endpoint** (C22-P2). The critique proposes F1 and PPV at Genus and Species on the two mocks, which would supersede decision 5 and narrow decision 31; the choice waits for the first figures on the new grid.

---

## 4. Reference

**Written before 2026-09-17.** The research questions still hold; the snapshot, the dataset matrix and the execution order describe the former design (Taudière endophytes, miaSim, `_cut` in the grid). For the current design read section 0 and `docs/objectives_design.md` §2–3.

### Main research questions (from the manuscript Results outline)

1. **Q1** — Effect of classification algorithm and its parameters
2. **Q2** — Effect of reference database and database simplification (Fungi-only, `_cut`, clustering)
3. **Q3** — Effect of consensus voting across methods/databases/parameters
4. **Q4** — Effect of ASV preprocessing: ITS extraction with ITSx (proposed 2026-09-11)
5. **Q5** — Effect of post-clustering the ASVs into OTUs (proposed 2026-09-15)

Plus the methodological scaffolding the manuscript announces:

- **D1** — Datasets: cross-validation, in silico, mock community, biological community (Bokulich 2020 four-data approach)
- **M1** — Performance metrics: TP/FP/FN/TN/MCC/ACC/F1/Precision/Recall (Hleap 2021 set)
- **M2** — Environmental metrics: CPU / wall time / CO₂eq

### Snapshot — what is already produced by the pipeline

| Piece                                                                    | Where                                                                              | Status                                                            |
| ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| ASV phyloseq `d_asv`                                                     | `store_dada2`, from `script_dada2.R`                                               | ✅ runs end-to-end (store dated 2024-12-19; rerun blocked by S0.3) |
| Fake taxa injection (shuffle + external) → `d_asv_for_assignation`       | `script_assign_taxo_parallel.R`                                                    | ✅ TN material is in place                                         |
| 4 methods × 6 DBs × bootstraps → `d_all_taxo` (combined `tax_table`)     | `store_assign_taxo`, via `tar_combine` + `combine_taxo_assignments`                | ❌ invalid: every target ran on `mini_Unite` (S6.1, S6.2)          |
| Per-target runtime / memory (`benchmark_costs`)                          | `store_assign_taxo`                                                                | 🔄 fixed and verified on the mini run; production pending (S6.5)  |
| Mock truth table `taxo_mock`                                             | `data/data_raw/metadata/taxo_mock.csv`                                             | ✅                                                                 |
| NA cleanup + `Gen_sp` construction                                       | `analysis/benchmark.qmd` § "Import value from store_assign_taxo"                   | ✅                                                                 |
| `tc_metrics_mock()` (TP/FP/FN/MCC/ACC/F1) per (method × db × rank)       | `benchmark.qmd` via `comparpq`                                                     | ✅ produced as `res_comp_tax`                                      |
| Three consensus strategies (unanimity, rel_majority, preference) applied | `benchmark.qmd` § "Create column using consensus..."                               | ✅ partial                                                         |
| Per-rank NA proportion plots                                             | `benchmark.qmd` § "Proportion of NA"                                               | ✅ exploratory plots                                               |
| Cross-validation helper `cross_val()` + targets pipeline                 | `R/cross_val.R` + `pipelines/cross_val.R` → `store_cross_val`                     | ❌ store invalid (S6.1, S6.4); code fixed                          |
| In silico notebook                                                       | `In_silico_simulation.qmd`                                                         | 🔄 D1b-B (miaSim) in progress                                     |
| Biological community dataset                                             | Taudière et al. 2018, doi:10.1016/j.funeco.2018.07.008 (endophytes; site × height) | ⚠️ chosen, fastq fetch pending                                    |
| `benchmark.qmd` ↔ parallel store wiring                                  | reads `tar_read(d_all_taxo, ...)` directly                                         | ✅ (Q1.1)                                                          |

### D1 — Multi-dataset coverage

Mock is done. The other three Bokulich-style datasets are partly missing.

| Dimension                   | Mock community                                                           | Cross-validation                                 | In silico                                                   | Biological community                                  |
| --------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------ | ----------------------------------------------------------- | ----------------------------------------------------- |
| **Dataset ID**              | D1                                                                       | D1a                                              | D1b                                                         | D1c                                                   |
| **Data origin**             | Lab-made community of known taxa                                         | Reference database itself (held-out folds)       | Computationally simulated reads                             | Real environmental samples (Taudière 2018 endophytes) |
| **Ground truth**            | Known taxonomy from culture collection                                   | DB labels (held-out)                             | Input FASTA taxonomy                                        | None                                                  |
| **Sequencing errors**       | Real (wet-lab)                                                           | None (DB sequences)                              | Simulated (InSilicoSeq MiSeq model) / None (miaSim)         | Real (wet-lab)                                        |
| **Community structure**     | Fixed, known abundances                                                  | Uniform (one seq per taxon)                      | Controlled (zero-inflated lognormal / neutral-niche models) | Unknown, natural                                      |
| **"Novel taxon" possible?** | No — all taxa are in the DB                                              | No — sequences are drawn from the DB             | No (D1b-A) / depends on model (D1b-B)                       | Yes — environmental taxa may be absent from any DB    |
| **Metrics computable**      | Full: TP/FP/FN/TN/MCC/F1/TAR/TDR                                         | good/wrong/NA proportions per fold               | Full (same as mock)                                         | Agreement-only: Jaccard, Bray-Curtis, richness        |
| **Main analytical risk**    | Mock may not represent natural diversity                                 | DB leakage (remove_tested = TRUE/FALSE variants) | Sequencing model may not capture real error profile         | No objective benchmark possible                       |
| **Script / store**          | `script_dada2.R` + `script_assign_taxo_parallel.R` → `store_assign_taxo` | `script_cross_val.R` → `store_cross_val`         | `In_silico_simulation.qmd` (D1b-B); `store_*_insilico` for D1b-A | New `store_*_endophyte` (planned)                     |
| **Status**                  | 🔄 code complete, results to regenerate (S6.5)                            | 🔄 code fixed, store to rerun (S6.5)             | 🔄 D1b-B (miaSim) in progress; D1b-A (InSilicoSeq) outlined | ❌ fastq fetch pending                                 |

### Suggested execution order (updated 2026-09-10)

A path that gives you a draftable Results section fast, then expands.

0. **Results validity (S7.4 → S6.5)** — ~~build the versioned reference databases (`derive_all_variants()`)~~ done 2026-09-11 with the fake reference (S7.4, S7.7), EUKARYOME files to rebuild after S7.8; validate on the `*_mini` projects (`source("R/run_project.R"); run_project("assign_taxo_mini")`), then rerun `dada2`, `assign_taxo` and `cross_val` in production and re-render the chapters. Every later step depends on it; phases 0–5 and S7.1–S7.3 are done.
1. **First pass (week-scale)** — Q1.1 → Q1.2 → Q1.3 → Q1.4 → M1.1 (TAR/TDR addition) → Q3.1 → Q3.2 → M2.1. End state: a complete mock-community Results section answering Q1 + Q3, with a cost panel and the agreed F1+MCC+TAR/TDR metric set. Manuscript can be partly drafted. *(Done except the M2.1 re-validation.)*
2. **Database axis (week-scale)** — Q2.1 → Q2.2 → Q2.3 → Q2.5. Adds Q2 to the draft. Remember to filter out `mini_*` rows (decision 4). *(Done except the Q2.5 re-validation.)*
3. **Robustness via CV (week-scale)** — D1a fully. Re-runs Q1/Q2 figures on CV data; the agreement between mock and CV is itself a result. *(Pipeline run; figures open.)*
4. **In silico** — D1b-A (InSilicoSeq) first because it reuses the existing DADA2 pipeline. Then D1b-B (miaSim) which only varies community structure.
5. **Biological** — D1c (Taudière 2018 endophytes). The fastq fetch is the first blocker; resolve it before standing up `store_dada2_endophyte`.
6. **Statistical layer** — Q1.6, Q1.7, Q2.5 (the cost-vs-DB-size analysis), M1.2 (Edgar-style MC/OC/EPQ for CV only).
7. **Polishing** — M2.2 (CO₂eq via greenAlgoR), M2.3 (DADA2 cost split), Q1.5b (IdTaxa-exclusion note in the methods table), C1–C4, Structure Phases 2–4.

Stop at step 2 if the goal is a short methods note; go through step 5 for the full four-dataset manuscript per Bokulich 2020.

---

## 5. Done log

Kept verbatim from the pre-2026-09-10 ROADMAP. **Every Q1, Q2, Q3, M2 and D1a result below was computed from stores invalidated by S6.1, S6.2 and S6.4**: the code items stay done, the numbers and figures must be regenerated (S6.5). Items marked ⚠️ also depended on S1.1.

### ✅ Phase 1 — Complete the mock-community Results section

| #   | Action                                                                                                                    | Command / location                                                                              | Blocker for                              | Status   |
| --- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------- | -------- |
| 1   | Re-run assign_taxo pipeline with v2 databases (renamed from v1_9_3 in Q2.3)                                               | `tar_make(script="script_assign_taxo_parallel.R", store="store_assign_taxo")` from project root | Q2 figures with Fungi-only ITS databases | ✅ Done  |
| 2   | Add IdTaxa exclusion note to the methods table in `Taxonomic assignation.md` (external Markdown file in Coffre_principal) | Edit manually — mark IdTaxa as "evaluated elsewhere, not in this study"                         | Q1.5b                                    | ✅ Done  |

### ✅ Phase 2 — Cross-validation

| #   | Action                                                                                                                      | Command / location                                                                                                               | Blocker for                               | Status  |
| --- | --------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ------- |
| 3   | **(Smoke test first)** Run CV pipeline with `cv_fold_tested = 2L` (already set in `config.R`) to verify the pipeline wiring | `tar_make(script="script_cross_val.R", store="store_cross_val")`                                                                 | Confirming D1a wiring before the long run | ✅ Done |
| 4   | Set `cv_fold_tested <- cv_fold_number` in `config.R` for the publication run, then re-run                                   | Edit `config.R`, then `tar_make(script="script_cross_val.R", store="store_cross_val")` (hours-scale)                            | D1a CV figures                            | ✅ Done |

### ✅ Phase 3 — Database derivation verification

| #   | Action                                                                                        | Command / location                                                                                              | Blocker for | Status  |
| --- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ----------- | ------- |
| 5   | Verify `make_databases.R::derive_all_variants()` end-to-end with new v2 source files         | `source("make_databases.R"); derive_all_variants()` — sources Unite.fasta, Euk_ITS_v2.fasta, Euk_SSU_v2.fasta  | C1 sign-off | ✅ Done |

### Q1 — Algorithm & parameters

Already producible from `res_comp_tax`. What's missing is the **systematic figure set** and a stats layer.

- [x] **Q1.1** — Re-point `benchmark.qmd` at `tar_read(d_all_taxo, store = "store_assign_taxo")` (drop the `_all_taxo` grep, drop the `rename_ranks_pq(...)` block). Required before anything else in the qmd works on the parallel store.
- [x] **Q1.2** — Lock the **per-rank × method** figure: ACC, F1, MCC across `c("dada2", "sintax", "lca", "blastn")` faceted by rank K→S. `plot_tc_metrics_mock()` already builds this; pin a final version and save as `figures/fig_q1_methods.{pdf,png}`.
- [x] **Q1.3** — **min_bootstrap sweep figure** for dada2 / sintax: F1 or MCC as a function of `min_bootstrap ∈ {0.4, 0.5, 0.6}`, per rank. Aggregate from `res_comp_tax` (`bootstrap` column already extracted there).
- [x] **Q1.4** — **Vote-algorithm sweep figure** for blastn: F1 or MCC across `c("rel_majority", "abs_majority", "unanimity")` × DB × rank. The data is already in `res_comp_tax`; needs a dedicated faceted panel.
- [x] **Q1.5** — ~~Add IdTaxa~~ **Decided out** (decision 1). Action: keep the `idtaxa` block commented out in `script_assign_taxo_parallel.R::values_map`, and add a one-paragraph justification in the Methods section ("IdTaxa requires a training step and is therefore excluded; see Murali et al. 2018 for an IdTaxa-focused comparison").
- [x] **Q1.5b** — Update the methods table in `Taxonomic assignation.md` to mark IdTaxa as "evaluated elsewhere, not in this study" so reviewers see the decision was deliberate.
- [x] **Q1.6** — Pairwise method comparison **table**: per-rank ACC/F1 means (±sd) across DBs via `knitr::kable`. **Win matrix**: `slice_max` per (rank, db) → `count(tax_level, algo)` → pivoted table. Both added to `benchmark.qmd` § "Q1.6".
- [x] **Q1.7** — Statistical test: `lme4::lmer(values ~ algo + (1|db))` vs null per rank. LRT via `anova(m_null, m_full)`; χ², df, p-value table with significance stars. Added to `benchmark.qmd` § "Q1.7". Requires `lme4` on search path.

### Q2 — Database & simplification

The DB axis spans six labels (Unite, Unite_Fungi, EUK_ITS_v2, EUK_ITS_v2_Fungi, EUK_ITS_v2_Fungi_cut, EUK_SSU_v2_Fungi_cut) (decision 7; `v1_9_3` names at the time). The qmd currently has DB on the y-axis of the NA plots but no systematic accuracy view.

- [x] **Q2.1** — **Per-DB accuracy figure**: all 4 methods faceted (`fig_q2_db.{pdf,png}`). Update `method_for_q2` to the best method from Q1.2 before finalising. Color = simplification type (full/Fungi/cut/Fungi+cut) via `db_meta` tibble.
- [x] **Q2.2** — **Simplification-effect table** + figure: `db_pair_delta()` helper computes Δ F1/MCC for three pairs; `knitr::kable` summary + `fig_q2_simplification.{pdf,png}`.
- [x] **Q2.3** — `EUK_ITS_v1_9_3_Fungi` and `EUK_ITS_v1_9_3_Fungi_cut` added to `values_map` in both `script_assign_taxo_parallel.R` and `values_map_for_qmd.R` (files confirmed present in both `dada2_format/` and `sintax_format/`). **Re-run `tar_make` to compute the new targets.** Note: `EUK_SSU_99_*` variants exist only in `sintax_format/` — needs a separate values_map block for non-dada2 methods if desired.
- [x] **Q2.4** — ~~Decide on mini DBs~~ **Decided out** (decision 4). `derive_mini()` stays in `make_databases.R` as a smoke-test tool only. Make sure no Results figure or table references `mini_*` rows. Filter them out of `values_map` selections when finalising figures (`dplyr::filter(!startsWith(db, "mini_"))`).
- [x] ⚠️ **Q2.5** — Discussion-side: tie DB-size to runtime via `benchmark_costs` (does a smaller DB pay for itself in accuracy/cost?). Figure code added to `benchmark.qmd` § "Q2.5 — DB size vs compute cost and accuracy" (`fig_q2_5_size_cost.{pdf,png}`).

### Q3 — Consensus voting

The qmd already builds three consensus columns. The manuscript table and `comparpq::resolve_taxo_conflict` document **five strategies × `strict` flag × `nb_agree_threshold`**.

- [x] **Q3.1** — Apply all five strategies × `strict ∈ {FALSE, TRUE}`: `unanimity`, `consensus`, `abs_majority`, `rel_majority`, `preference`. Loop in benchmark.qmd (extra_consensus_configs) adds 9 new columns covering all missing combinations.
- [x] **Q3.2** — **Consensus vs single-method figure**: F1/MCC per rank with consensus strategies plotted alongside the best single methods (conservatism order: preference→rel_majority→abs_majority→consensus→unanimity). `figures/fig_q3_consensus_vs_single.{pdf,png}`.
- [x] **Q3.3** — `nb_agree_threshold` sweep on `rel_majority` with values **1, 2, 3** (decision 6). `rel_majority_nb2_consensus` and `rel_majority_nb3_consensus` columns added; `figures/fig_q3_nb_threshold.{pdf,png}`.
- [x] **Q3.4** — `all_consensus_suffixes` vector drives the `ranks_df` loop; `tc_metrics_mock` now runs on all 12 consensus columns. `is_consensus`, `consensus_strict`, `nb_agree` columns added to `res_comp_tax` for downstream filtering.
- [x] **Q3.5** — **Method×DB matrix for `preference`**: `geom_tile` heatmap of F1 (Gen_sp) with method on y and DB on x; the preferred cell (sintax × EUK_ITS_v1_9_3) highlighted with a red border; preference-consensus F1 annotated. `figures/fig_q3_heatmap.{pdf,png}`.

### D1a — Cross-validation

- [x] Wire `cross_val()` into its own targets script (`script_cross_val.R` → `store_cross_val`). Inputs: each DB in `values_map$db`; each method. Outputs: a tibble per (method, db) with good/wrong/NA proportions per rank (averaged over folds via `cv_to_tidy()`). Note: dada2 single-bootstrap branch in `cross_val()` does not apply the bootstrap filter (known upstream limitation).
- [x] Add the **leaked** variant (set `remove_tested_sequences = FALSE` in `cross_val`). Both `remove_tested = TRUE` (standard) and `FALSE` (leaked) rows are in `cv_values_map`; the `leaked_suffix` column drives target naming.
- [x] Aggregation target: `cv_results` via `tarchetypes::tar_combine(bind_rows(!!!.x))`. Each per-(method,db,variant) tibble has columns `tax_level`, `mean`, `sd`, `metric`, `method`, `db`, `remove_tested`, `min_bootstrap`. Run with: `tar_make(script = "script_cross_val.R", store = "store_cross_val")`.
- [x] Smoke-test size control: `max_seq` parameter added to `cross_val()` (subsamples the DB before folding). Wired through `run_cv()` in `script_cross_val.R` via `cv_max_seq` in `config.R`. Three publication knobs in `config.R`: `cv_fold_number = 10L`, `cv_fold_tested = 2L` (raise to `cv_fold_number` for publication), `cv_max_seq = 100L` (set to `NULL` for full DB). Fix also applied: duplicate sequences within a fold are deduplicated before `create_fake_pq_from_refseq()` to avoid MiscMetabar's refseq validation error on EUK SSU databases.

### M1 — Performance metrics

The set used in `tc_metrics_mock` already covers TP/FP/FN/TN/MCC/ACC/F1. The manuscript notes also mention Bokulich's TAR/TDR, Edgar's MC/OC/EPQ, and Bokulich-2018 over-/under-classification rates.

- [x] **M1.1** — Headline metric set fixed (decision 5): **F1 + MCC + TAR/TDR per rank** for mock-community results. TP/FP/FN/TN go to SM only. TAR (=PPV) and TDR (=TPR) added as `bind_rows` aliases after `res_comp_tax` creation in `benchmark.qmd`. `vote_algorithm` and `bootstrap_num` columns also added there for cleaner downstream filtering.
- [x] **M1.3** — Confirm that the **fake taxa** (`add_shuffle_seq_pq` + `add_external_seq_pq`) are correctly counted as the TN denominator across methods — there is a comment in `ieauieau_tmp.R` (`fake_taxa_cond`) suggesting an in-progress reimplementation. Resolve which version is in `comparpq` now. **Resolved:** `ieauieau_tmp.R` does not exist on disk; the only implementation is in `comparpq/R/compare_taxo.R::tc_metrics_mock_vec()`, which correctly sets `fake_taxa_cond <- taxa_names(physeq) %in% fake_taxa_names` (matching `^fake_|^external_`) and counts TN as NA assignments among those taxa.

### M2 — Environmental metrics

`benchmark_costs` target already aggregates `wall_time_s`, `peak_resident_mb`, `mean_cpu_pct` per (method, db, bootstrap, vote). Missing: usage in the analysis.

- [x] ⚠️ **M2.1** — Plot **accuracy vs cost**: x = `wall_time_s` (from `tar_read(benchmark_costs, ...)`), y = F1 (at Gen_sp), color = method, labels = DB. `fig_m2_cost_species.{pdf,png}` (main) + `fig_m2_cost_allranks.{pdf,png}` (SM, all ranks in one row).
- [x] ⚠️ **M2.2** — CO₂eq via `greenAlgoR` (the pqverse package listed in the workspace CLAUDE.md). Feed `benchmark_costs` through it; one row per assignment target.
- [x] ⚠️ **M2.3** — Also log `benchmark_costs` for the **dada2 store** so the DADA2 preprocessing cost is attributable separately from the assignment cost (currently only `data_final/autometric_log_assign_taxo.txt` is post-processed; `autometric_log_dada2.txt` is dormant).

### Cross-cutting / housekeeping

- [x] **C2** — Update `analysis/benchmark.qmd` so it loads `dbpq` (currently sources `comparpq` files only) — needed for `dbpq::format2dada2` if any rerun is triggered from the qmd.
- [x] **C3** — `script_dada2.R` still has inline copies of the `config.R` constants. Migrate it to `source(here("config.R"))` next time you re-execute the DADA2 store.
